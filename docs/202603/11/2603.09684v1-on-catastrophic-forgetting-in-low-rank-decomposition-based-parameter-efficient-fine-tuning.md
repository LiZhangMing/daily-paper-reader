---
title: On Catastrophic Forgetting in Low-Rank Decomposition-Based Parameter-Efficient Fine-Tuning
title_zh: 关于基于低秩分解的参数高效微调中的灾难性遗忘研究
authors: "Muhammad Ahmad, Jingjing Zheng, Yankai Cao"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09684v1"
tags: ["query:peft"]
score: 10.0
evidence: 研究基于低秩分解的PEFT方法（如LoRA）中的灾难性遗忘问题
tldr: 本研究深入探讨了基于低秩分解的参数高效微调（PEFT）方法在顺序学习中的灾难性遗忘问题。通过实证分析发现，遗忘程度与更新子空间的几何结构和参数化方式密切相关。研究表明，张量分解（如LoRETTA）和结构对齐参数化（如WeGeFT）能比传统LoRA更有效地保留预训练表示，从而缓解任务间的干扰，为连续学习场景下的模型适配提供了关键的指导原则。
selection_source: fresh_fetch
motivation: 旨在填补关于低秩分解微调方法在顺序学习中如何产生及缓解灾难性遗忘的研究空白。
method: 通过对比实验研究了不同低秩更新子空间的几何特性及其对模型表示保留能力的影响。
result: 实验证明张量化分解和结构对齐的参数化方法在极低参数预算下能显著降低任务干扰并减少遗忘。
conclusion: 更新子空间的设计是决定连续学习效果的核心要素，合理的参数化策略能有效平衡微调效率与知识保留。
---

## 摘要
基于低秩分解的参数高效微调（PEFT），如 LoRA，已成为适配大型预训练模型的标准方法。然而，其在顺序学习（sequential learning）中的表现——特别是关于灾难性遗忘的问题——尚未得到充分理解。在这项工作中，我们通过实证研究表明，遗忘受更新子空间的几何结构和参数化方式的强烈影响。虽然将更新限制在小型共享矩阵子空间的方法通常会受到任务干扰的影响，但基于张量的分解（如 LoRETTA）通过在极紧凑的预算内捕捉更丰富的结构信息来缓解遗忘，而结构对齐的参数化（如 WeGeFT）则能保留预训练表示。我们的研究结果强调了更新子空间设计是持续学习中的关键因素，并为在顺序设置中选择高效的适配策略提供了实践指导。

## Abstract
Parameter-efficient fine-tuning (PEFT) based on low-rank decomposition, such as LoRA, has become a standard for adapting large pretrained models. However, its behavior in sequential learning -- specifically regarding catastrophic forgetting -- remains insufficiently understood. In this work, we present an empirical study showing that forgetting is strongly influenced by the geometry and parameterization of the update subspace. While methods that restrict updates to small, shared matrix subspaces often suffer from task interference, tensor-based decompositions (e.g., LoRETTA) mitigate forgetting by capturing richer structural information within ultra-compact budgets, and structurally aligned parameterizations (e.g., WeGeFT) preserve pretrained representations. Our findings highlight update subspace design as a key factor in continual learning and offer practical guidance for selecting efficient adaptation strategies in sequential settings.

---

## 论文详细总结（自动生成）

以下是对论文《On Catastrophic Forgetting in Low-Rank Decomposition-Based Parameter-Efficient Fine-Tuning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在顺序学习（Sequential Learning）场景下，基于低秩分解的参数高效微调（PEFT）方法（如 LoRA）是否会加剧或缓解“灾难性遗忘”？
*   **研究动机**：虽然 PEFT 在单任务适配中表现优异，但其在处理一系列连续任务时的知识保留能力尚不明确。研究者希望探究更新子空间（Update Subspace）的几何特性和参数化方式如何影响模型对旧知识的记忆。

### 2. 论文提出的方法论
论文通过实证研究对比了四种具有代表性的低秩 PEFT 架构，分析它们在连续学习中的表现：
*   **核心思想**：遗忘程度与更新子空间的“自由度”和“结构对齐”密切相关。
*   **关键技术对比**：
    *   **LoRA**：将权重更新分解为两个低秩矩阵。研究发现其遗忘程度随秩（Rank）增加而降低，因为更大的子空间减少了任务间的方向冲突。
    *   **PiSSA**：仅更新预训练权重的奇异值和奇异向量。实验发现它在低秩下遗忘最严重，因为它直接干扰了预训练表示的核心空间。
    *   **LoRETTA**：采用**张量链分解（Tensor Train Decomposition）**。通过将权重重塑为高维张量，在极低参数量下捕捉更丰富的结构信息，从而缓解遗忘。
    *   **WeGeFT**：将更新限制在与预训练权重对齐的子空间内，并通过跨层参数共享减少可训练参数。这种结构对齐有助于保留原始表示。

### 3. 实验设计
*   **模型架构**：以在 ImageNet-1K 上预训练的 **Vision Transformer (ViT-Base 和 ViT-Large)** 为基础模型。
*   **数据集与场景**：构建了一个包含四个顺序任务的视觉基准测试：
    1.  **CUB-200-2011**（鸟类物种分类，细粒度识别）。
    2.  **EuroSat**（土地利用分类，地理空间图像）。
    3.  **Intel Image Classification**（自然场景识别）。
    4.  **Sports Image Classification**（体育类别识别）。
*   **对比基准（Baselines）**：全参数微调（Full Fine-tuning, FF）、LoRA、PiSSA、LoRETTA、WeGeFT。
*   **评估指标**：平均遗忘率（Average Forgetting）、最终准确率（Final Accuracy）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **超参数细节**：附录中提到使用了 Adam 优化器，学习率在 0.0001 到 0.01 之间，每个任务训练 10 个 Epoch，Batch Size 为 15。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两种不同规模的模型（ViT-Base 和 ViT-Large）。
    *   针对 LoRA、PiSSA 和 WeGeFT 进行了不同秩（Rank $r=2$ 到 $64$）的消融实验。
    *   对比了从 54K 到 303M 不等的可训练参数量级。
*   **充分性评价**：实验设计较为系统，通过控制变量（如相同的任务序列、统一的评估指标）客观地展示了不同 PEFT 方法在连续学习中的性能趋势。虽然任务序列长度（4个任务）不算极长，但涵盖了从细粒度到宏观场景的领域偏移，具有代表性。

### 6. 主要结论与发现
*   **更新灵活性与遗忘的关系**：全参数微调由于更新空间不受限，遗忘率相对较低；而 LoRA 等方法在秩极小时，由于任务被迫共享有限的更新方向，会导致严重的任务干扰。
*   **PiSSA 的局限性**：PiSSA 虽然在单任务中表现好，但在顺序学习中表现最差，因为它破坏了预训练权重的奇异值空间。
*   **张量化与对齐的优势**：LoRETTA（张量分解）和 WeGeFT（子空间对齐）在极低参数预算下表现出最强的抗遗忘能力。
*   **设计原则**：缓解遗忘的关键在于：要么提供足够的更新灵活性（增加秩），要么采用能保持预训练表示结构的参数化设计（如张量化或结构对齐）。

### 7. 优点
*   **视角独特**：从“更新子空间几何”的角度解释了 PEFT 方法在连续学习中的行为差异。
*   **实用指导**：为需要在生产环境中进行连续适配的开发者提供了选择 PEFT 策略的依据（例如，在参数受限时优先选择 LoRETTA 或 WeGeFT 而非低秩 LoRA）。
*   **对比全面**：不仅对比了传统的矩阵分解，还引入了前沿的张量分解和权重生成方法。

### 8. 不足与局限
*   **领域局限**：实验完全集中在计算机视觉（ViT）领域，未在自然语言处理（LLM）等 LoRA 应用更广泛的领域进行验证。
*   **任务序列长度**：仅测试了 4 个任务的序列，对于更长生命周期的持续学习（如数十个任务）的表现尚不可知。
*   **缺乏理论证明**：研究主要基于实证观察，缺乏关于子空间几何与遗忘之间关系的严格数学证明。

（完）
