---
title: "TrainDeeploy: Hardware-Accelerated Parameter-Efficient Fine-Tuning of Small Transformer Models at the Extreme Edge"
title_zh: TrainDeeploy：极端边缘端小型 Transformer 模型的硬件加速参数高效微调
authors: "Run Wang, Victor J. B. Jung, Philip Wiese, Francesco Conti, Alessio Burrello, Luca Benini"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09511v1"
tags: ["query:peft"]
score: 9.0
evidence: 针对 Transformer 模型的硬件加速参数高效微调
tldr: 本研究针对极低功耗边缘设备上深度神经网络微调面临的计算与内存挑战，提出了TrainDeeploy框架。该框架在异构RISC-V SoC上实现了首个支持CNN和Transformer模型的完整链上训练流水线，集成了层级微调和LoRA等参数高效策略。实验表明，TrainDeeploy在保持高能效的同时，显著降低了内存占用和参数量，为极端边缘侧的长期自适应学习提供了可能。
selection_source: fresh_fetch
motivation: 针对极低功耗边缘设备在处理Transformer模型反向传播时面临的计算资源受限和内存需求过高的问题。
method: 提出TrainDeeploy框架，通过硬件加速和LoRA等参数高效微调策略，在异构SoC上实现端到端的链上训练。
result: "在RISC-V SoC上实现了每秒11张图像的CCT微调速度，LoRA将动态内存使用降低了23%，训练参数量减少了15倍。"
conclusion: TrainDeeploy成功扩展了边缘侧训练框架的适用范围，证明了在极端受限硬件上高效微调Transformer模型的可行性。
---

## 摘要
深度神经网络的设备端调优能够在保护数据隐私的同时，实现边缘端的长期自适应。然而，反向传播的高计算和内存需求给超低功耗、内存受限的极端边缘设备带来了重大挑战。由于架构复杂性和计算规模，这些挑战在基于注意力机制的模型中被进一步放大。我们提出了 TrainDeeploy，这是一个在异构超低功耗片上系统（SoC）上统一高效推理和设备端训练的框架。TrainDeeploy 为极端边缘 SoC 提供了首个完整的设备端训练流水线，支持卷积神经网络（CNN）和 Transformer 模型，并提供多种训练策略，如选择性逐层微调和低秩自适应（LoRA）。在基于 RISC-V 的异构 SoC 上，我们展示了紧凑型卷积 Transformer（CCT）的首个端到端设备端微调，实现了每秒高达 11 张图像的训练速度。我们展示了与全量反向传播相比，LoRA 将动态内存使用量减少了 23%，可训练参数和梯度数量减少了 15 倍，内存传输量减少了 1.6 倍。TrainDeeploy 在 CCT（0.28M 参数，71-126M FLOPs）上实现了高达 4.6 FLOP/cycle 的性能，在 Deep-AE（0.27M 参数，0.8M FLOPs）上实现了高达 13.4 FLOP/cycle 的性能，同时扩展了先前框架的范围，以支持在极端边缘平台上对 CNN 和 Transformer 模型进行参数高效调优。

## Abstract
On-device tuning of deep neural networks enables long-term adaptation at the edge while preserving data privacy. However, the high computational and memory demands of backpropagation pose significant challenges for ultra-low-power, memory-constrained extreme-edge devices. These challenges are further amplified for attention-based models due to their architectural complexity and computational scale. We present TrainDeeploy, a framework that unifies efficient inference and on-device training on heterogeneous ultra-low-power System-on-Chips (SoCs). TrainDeeploy provides the first complete on-device training pipeline for extreme-edge SoCs supporting both Convolutional Neural Networks (CNNs) and Transformer models, together with multiple training strategies such as selective layer-wise fine-tuning and Low-Rank Adaptation (LoRA). On a RISC-V-based heterogeneous SoC, we demonstrate the first end-to-end on-device fine-tuning of a Compact Convolutional Transformer (CCT), achieving up to 11 trained images per second. We show that LoRA reduces dynamic memory usage by 23%, decreases the number of trainable parameters and gradients by 15x, and reduces memory transfer volume by 1.6x compared to full backpropagation. TrainDeeploy achieves up to 4.6 FLOP/cycle on CCT (0.28M parameters, 71-126M FLOPs) and up to 13.4 FLOP/cycle on Deep-AE (0.27M parameters, 0.8M FLOPs), while expanding the scope of prior frameworks to support both CNN and Transformer models with parameter-efficient tuning on extreme-edge platforms.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **TrainDeeploy**，这是一个专为极端边缘（Extreme Edge）设备设计的硬件加速、参数高效的微调框架，首次在超低功耗 RISC-V 异构 SoC 上实现了 Transformer 模型的端到端设备端训练（On-Device Training, ODT）。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在资源极度受限（内存仅为 KB/MB 级别、功耗极低）的边缘微控制器（MCU）上，高效地进行深度学习模型（尤其是复杂的 Transformer）的反向传播训练。
*   **研究动机**：
    *   **隐私与自适应**：边缘端训练可实现长期自适应和个性化，同时无需将私密数据上传云端。
    *   **计算与内存瓶颈**：反向传播需要存储中间激活值和梯度，计算量巨大，传统的 CNN 训练框架难以直接扩展到架构更复杂的 Transformer 模型。
    *   **现有工具不足**：目前的边缘训练框架大多仅支持 CNN，且缺乏针对异构硬件（如带有加速器的 SoC）的端到端内存优化和自动微分支持。

### 2. 方法论
TrainDeeploy 建立在推理编译器 Deeploy 之上，通过以下关键技术实现从推理到训练的扩展：
*   **静态训练图构建**：将 PyTorch 模型导出为 ONNX，利用自动微分引擎生成包含前向、反向和优化器更新的完整静态计算图。
*   **参数高效微调（PEFT）集成**：原生支持 **LoRA（低秩自适应）**。通过将权重更新限制在低秩矩阵 $A$ 和 $B$ 中，大幅减少了需要存储的梯度数量和可训练参数量。
*   **异构存储与算力管理**：
    *   **内存调度**：采用 2D 装箱算法（TetriSched）在 L1（紧耦合内存）、L2（片上 SRAM）和 L3（外部存储）之间进行静态张量分配和分块（Tiling）。
    *   **硬件加速**：将计算密集型的矩阵乘法（GEMM）和卷积操作下放至片上硬件加速器（如 RedMulE，一个 FP32 脉动阵列加速器）。
*   **编译器后端**：自动生成针对 RISC-V 核心和加速器优化的 C 代码。

### 3. 实验设计
*   **目标模型**：**CCT-2**（紧凑型卷积 Transformer），包含 0.28M 参数。
*   **硬件平台**：基于 PULP 架构的异构 SoC（8 核 RISC-V 簇 + RedMulE 加速器），在 GVSoC 仿真器中运行。
*   **数据集与场景**：
    *   **少样本迁移学习**：CIFAR-10 预训练模型迁移至 MNIST 和 EuroSAT（50-shot 任务）。
*   **对比策略**：
    *   **LP（线性探测）**：仅训练分类头。
    *   **FT-1/FT-2**：全量微调最后 1 或 2 个 Transformer 块。
    *   **LoRA-1/LoRA-2**：对最后 1 或 2 个块应用 LoRA（秩 $r=4$）。
*   **Benchmark 对比**：与 PULP-TrainLib、POET、MiniLearn 和 TTE 等最先进的边缘训练框架进行对比。

### 4. 资源与算力
*   **算力平台**：模拟 360 MHz 的异构 SoC。
*   **加速器**：RedMulE（12x4 FP32 脉动阵列）。
*   **训练细节**：采用单样本（Batch Size = 1）训练，所有计算（权重、激活、梯度、优化器状态）均使用 **FP32** 精度以保证稳定性。
*   **论文未明确说明**：未提及在物理 GPU 上进行模拟的具体耗时，但详细记录了在目标 SoC 上的运行周期和吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同的微调策略，并在两个不同的迁移学习数据集上进行了 30 次重复实验以确保统计鲁棒性。
*   **充分性**：实验对比了纯 CPU 簇执行与硬件加速执行的差异，并详细分析了内存占用和数据迁移量。
*   **客观性**：通过与多个 SOTA 框架在相同或相似任务（如 Deep-AE 模型）上的对比，证明了其在计算效率（FLOP/cycle）上的领先地位。

### 6. 主要结论与发现
*   **性能突破**：在 CCT 模型上实现了每秒 **11 次梯度更新** 的吞吐量。
*   **LoRA 的优势**：相比全量微调，LoRA 将可训练参数和梯度减少了 **15 倍**，动态内存占用降低 **23%**，外部存储数据传输减少 **1.6 倍**，且准确率损失极小（甚至在某些任务上更高）。
*   **硬件加速效果**：RedMulE 加速器相比 8 核 RISC-V 簇实现了 **2.3x 至 3.5x** 的端到端加速。
*   **计算效率**：在 CCT 上达到 4.6 FLOP/cycle，在小型网络（Deep-AE）上达到 13.4 FLOP/cycle，远超现有框架。

### 7. 优点
*   **端到端自动化**：从 PyTorch 模型到异构硬件 C 代码的完整流水线，降低了边缘训练的部署门槛。
*   **异构感知**：深度结合了多级存储层次和专用加速器，解决了边缘端最核心的内存瓶颈。
*   **通用性**：同时支持 CNN 和 Transformer，不依赖于特定的稀疏化或剪枝技巧（虽然可以结合使用）。

### 8. 不足与局限
*   **模型规模限制**：虽然支持 Transformer，但仍局限于“小型”模型（如 CCT），对于参数量更大的 LLM 仍面临巨大挑战。
*   **仿真依赖**：主要实验结果基于 GVSoC 仿真，虽然参考了真实的硅片原型数据，但在实际物理芯片上的热管理和功耗波动尚未完全体现。
*   **Batch Size 限制**：目前主要针对 Batch Size 为 1 的场景，对于需要更大 Batch Size 来稳定收敛的任务，内存压力将剧增。

（完）
