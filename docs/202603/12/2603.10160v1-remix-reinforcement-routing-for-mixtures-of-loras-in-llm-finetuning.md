---
title: "ReMix: Reinforcement routing for mixtures of LoRAs in LLM finetuning"
title_zh: ReMix：大语言模型微调中 LoRA 混合的强化路由
authors: "Ruizhong Qiu, Hanqing Zeng, Yinglong Xia, Yiwen Meng, Ren Chen, Jiarui Feng, Dongqi Fu, Qifan Wang, Jiayi Liu, Jun Xiao, Xiangjun Fan, Benyu Zhang, Hong Li, Zhining Liu, Hyunsik Yoo, Zhichen Zeng, Tianxin Wei, Hanghang Tong"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10160v1"
tags: ["query:peft"]
score: 10.0
evidence: LLM 微调中 LoRA 混合体的强化路由
tldr: 针对Mixture-of-LoRAs模型中路由权重极度不平衡、限制模型表达能力的问题，本文提出ReMix。该方法通过引入不可学习的路由权重确保所有激活的LoRA同等有效，并利用强化学习中的RLOO技术进行梯度估计，从而在保持参数效率的同时显著提升了微调性能。
selection_source: fresh_fetch
motivation: 现有的Mixture-of-LoRAs路由权重分布极不均匀，导致少数LoRA主导模型，严重限制了多LoRA组合的表达能力。
method: 提出ReMix框架，采用不可学习的等额路由权重，并结合强化学习中的RLOO梯度估计器来优化路由策略。
result: 实验表明，在激活参数量相当的情况下，ReMix在多项任务上的表现显著优于现有的最先进参数高效微调方法。
conclusion: 通过重新设计路由机制并引入强化学习优化，ReMix有效解决了LoRA混合模型中的权重失衡问题，提升了LLM微调的性能。
---

## 摘要
低秩自适应（LoRA）是一种参数高效的微调技术，通过在预训练模型中注入可训练的低秩矩阵，使其能够适应新任务。LoRA 混合（Mixture-of-LoRAs）模型通过将每一层的输入路由到该层的一小部分专门 LoRA 子集中，从而高效地扩展神经网络。现有的 LoRA 混合路由层为每个 LoRA 分配一个学习到的路由权重，以实现路由层的端到端训练。尽管在实验中表现良好，但我们观察到在实践中，LoRA 之间的路由权重通常极度不平衡，往往只有一两个 LoRA 占据主导地位。这在本质上限制了有效 LoRA 的数量，从而严重阻碍了现有 LoRA 混合模型的表达能力。在这项工作中，我们将这一弱点归因于可学习路由权重的本质，并重新思考了路由层的基本设计。为了解决这一关键问题，我们提出了一种名为 ReMix（Reinforcement Routing for Mixture-of-LoRAs）的新型路由层设计。我们的核心思想是使用不可学习的路由权重，以确保所有激活的 LoRA 具有同等的效力，避免任何 LoRA 主导路由权重。然而，由于采用了不可学习的路由权重，我们的路由层无法直接通过梯度下降进行训练。因此，我们通过采用强化留一法（RLOO）技术，为路由层进一步提出了一种无偏梯度估计器，其中我们将监督损失视为奖励，将路由层视为强化学习中的策略。我们的梯度估计器还能够扩展训练计算量，以提升 ReMix 的预测性能。大量实验表明，在激活参数数量相当的情况下，我们提出的 ReMix 显著优于最先进的参数高效微调方法。

## Abstract
Low-rank adapters (LoRAs) are a parameter-efficient finetuning technique that injects trainable low-rank matrices into pretrained models to adapt them to new tasks. Mixture-of-LoRAs models expand neural networks efficiently by routing each layer input to a small subset of specialized LoRAs of the layer. Existing Mixture-of-LoRAs routers assign a learned routing weight to each LoRA to enable end-to-end training of the router. Despite their empirical promise, we observe that the routing weights are typically extremely imbalanced across LoRAs in practice, where only one or two LoRAs often dominate the routing weights. This essentially limits the number of effective LoRAs and thus severely hinders the expressive power of existing Mixture-of-LoRAs models. In this work, we attribute this weakness to the nature of learnable routing weights and rethink the fundamental design of the router. To address this critical issue, we propose a new router designed that we call Reinforcement Routing for Mixture-of-LoRAs (ReMix). Our key idea is using non-learnable routing weights to ensure all active LoRAs to be equally effective, with no LoRA dominating the routing weights. However, our routers cannot be trained directly via gradient descent due to our non-learnable routing weights. Hence, we further propose an unbiased gradient estimator for the router by employing the reinforce leave-one-out (RLOO) technique, where we regard the supervision loss as the reward and the router as the policy in reinforcement learning. Our gradient estimator also enables to scale up training compute to boost the predictive performance of our ReMix. Extensive experiments demonstrate that our proposed ReMix significantly outperform state-of-the-art parameter-efficient finetuning methods under a comparable number of activated parameters.

---

## 论文详细总结（自动生成）

以下是对论文《ReMix: Reinforcement routing for mixtures of LoRAs in LLM finetuning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是大语言模型（LLM）微调中的**参数高效微调（PEFT）**，特别是**LoRA 混合（Mixture-of-LoRAs）**架构。
*   **研究动机**：现有的 Mixture-of-LoRAs 方法通过路由层（Router）动态选择多个 LoRA 模块。然而，作者通过理论证明和实验观察发现，这些模型普遍存在**“路由权重坍缩”（Routing Weight Collapse）**现象：即使设置激活 $k > 1$ 个 LoRA，路由权重也会迅速集中在某一个 LoRA 上（权重接近 1），而其他 $k-1$ 个 LoRA 的权重微乎其微。
*   **核心问题**：这种不平衡导致其他激活的 LoRA 实际上未发挥作用，浪费了计算资源，并严重限制了模型的表达能力。

### 2. 方法论：ReMix 框架
为了解决权重坍缩问题，作者提出了 **ReMix**，其核心思想是将路由权重从“可学习的连续值”改为“固定的常数”。
*   **核心思想**：强制给所有被选中的 $k$ 个 LoRA 分配相等的常数权重 $\omega$（例如 $\omega = 2/kr$），未选中的权重为 0。这保证了所有激活的 LoRA 贡献均等，有效支持度（ESS）始终等于 $k$。
*   **关键技术细节**：
    *   **非微分路由训练**：由于权重是常数，无法通过标准反向传播训练路由参数。作者将其建模为**强化学习（RL）**问题，将微调损失（SFT Loss）的负值视为奖励（Reward）。
    *   **RLOO 梯度估计器**：采用 **REINFORCE Leave-One-Out (RLOO)** 技术。在训练时，对路由策略进行 $M$ 次采样，通过留一法（Leave-One-Out）计算基准线以降低方差，从而获得路由参数的无偏梯度估计。
    *   **推理策略**：在推理阶段，作者证明了只要路由层训练充分，采用 **Top-$k$ 选择**（选择概率最高的 $k$ 个 LoRA）是理论上的最优策略。

### 3. 实验设计
*   **数据集**：
    *   **GSM8K**：评估数学推理能力。
    *   **HumanEval**：评估代码生成能力（在 CodeAlpaca 上训练）。
    *   **ARC-c**：评估知识召回能力。
*   **基准模型（Base Model）**：Llama 3 8B。
*   **对比方法（Baselines）**：
    *   **传统 PEFT**：Prefix Tuning, Prompt Tuning, P-Tuning, (IA)³。
    *   **LoRA 变体**：LoRA, DoRA, rsLoRA。
    *   **混合架构**：VB-LoRA, MixLoRA, HydraLoRA。
*   **实验设置**：在相同的参数预算下进行公平比较，搜索各方法的最佳超参数。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号和数量，但提到所有实验均在 **LLaMA-Factory** 框架下训练，并使用 **OpenCompass** 进行评估。
*   **训练时长**：在训练效率对比中（表 4），ReMix 在特定任务上的总训练时间约为 **1 小时 28 分钟**，相比 MixLoRA（1 小时 13 分钟）增加了约 **10%** 的计算开销。这主要是由于 RLOO 需要进行多次采样计算。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了数学、代码、常识三大主流任务，实验设计较为全面。
*   **消融实验**：作者针对 RLOO 梯度估计器、Top-$k$ 推理策略分别做了消融，证明了各组件的必要性。
*   **扩展性分析**：研究了激活 LoRA 数量 $k$ 的影响，以及采样次数 $M$（训练计算量）对性能的提升作用。
*   **公平性**：通过控制激活参数总量（Params）来对比不同方法，确保了比较的客观性。

### 6. 主要结论与发现
*   **性能领先**：ReMix 在所有基准测试中均显著优于 SOTA 方法。在平均准确率上，比最强的混合架构基准（MixLoRA）高出约 **3.34%**。
*   **解决坍缩**：成功解决了路由权重坍缩问题，使多个 LoRA 能够真正协同工作。
*   **计算可扩展性**：ReMix 的性能随训练采样数 $M$ 的增加而稳步提升，这意味着可以通过投入更多训练算力来换取更好的模型表现，而传统方法（如 MixLoRA）无法通过这种方式获益。

### 7. 优点（亮点）
*   **理论支撑强**：论文不仅提出了方法，还给出了关于路由坍缩的理论证明（定理 1）以及 Top-$k$ 选择最优性的证明（定理 2）。
*   **简单高效**：ReMix 的架构改动极小，不增加推理时的额外计算负担（与现有 MoLoRA 相同）。
*   **鲁棒性**：实验表明 ReMix 对常数权重 $\omega$ 的具体取值不敏感，易于部署。

### 8. 不足与局限
*   **训练开销**：由于引入了 RL 采样过程，训练时间比基于梯度的传统方法略长（约 10% 额外开销）。
*   **模型覆盖**：实验主要集中在 Llama 3 8B 上，虽然这是主流模型，但未在更大规模（如 70B+）或其他架构（如 MoE 原生模型）上验证。
*   **RL 调参**：强化学习方法通常比直接梯度下降更依赖超参数（如采样数 $M$），尽管 RLOO 降低了方差，但在不同任务上的稳定性仍需更多验证。

（完）
