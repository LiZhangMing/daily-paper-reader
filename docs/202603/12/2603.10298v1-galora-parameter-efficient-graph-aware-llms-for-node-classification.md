---
title: "GaLoRA: Parameter-Efficient Graph-Aware LLMs for Node Classification"
title_zh: GaLoRA：用于节点分类的参数高效图感知大语言模型
authors: "Mayur Choudhary, Saptarshi Sengupta, Katerina Potika"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10298v1"
tags: ["query:peft"]
score: 9.0
evidence: GaLoRA：参数高效图感知大模型
tldr: "针对文本属性图（TAGs）中的节点分类任务，本文提出了GaLoRA框架。该框架旨在将图结构信息高效集成到大语言模型（LLMs）中，以同时捕捉语义和结构特征。GaLoRA通过参数高效的微调方法，仅需全量微调0.24%的参数量，即可在多个真实数据集上达到最先进的性能，为处理复杂图数据提供了高效且强大的解决方案。"
selection_source: fresh_fetch
motivation: 旨在解决如何在大语言模型中有效融合图结构信息，以提升文本属性图上的节点分类性能。
method: 提出了GaLoRA框架，通过参数高效的微调机制将图结构特征整合进LLMs的表示学习过程中。
result: "在三个真实数据集上的实验表明，GaLoRA仅用0.24%的参数量就实现了与全量微调相当的分类效果。"
conclusion: GaLoRA证明了在保持极低参数开销的同时，通过结构感知微调能显著增强LLMs处理图数据的能力。
---

## 摘要
大语言模型（LLMs）的迅速崛起及其捕捉语义关系的能力，使其在广泛的应用中得到采用。文本属性图（TAGs）是一个显著的例子，其中 LLMs 可以与图神经网络结合，以提高节点分类的性能。在 TAGs 中，每个节点都与文本内容相关联，此类图常见于社交网络、引用图、推荐系统等各种领域。有效地从 TAGs 中学习将能够更好地表示图的结构和文本特征，并改善相关领域的决策。我们提出了 GaLoRA，这是一个将结构信息集成到 LLMs 中的参数高效框架。GaLoRA 在 TAGs 的节点分类任务上展示了具有竞争力的性能，其参数量仅为全量 LLM 微调所需参数量的 0.24%，却能与最先进的模型相媲美。我们在三个真实世界数据集上进行了实验，以展示 GaLoRA 在结合 TAGs 结构和语义信息方面的有效性。

## Abstract
The rapid rise of large language models (LLMs) and their ability to capture semantic relationships has led to their adoption in a wide range of applications. Text-attributed graphs (TAGs) are a notable example where LLMs can be combined with Graph Neural Networks to improve the performance of node classification. In TAGs, each node is associated with textual content and such graphs are commonly seen in various domains such as social networks, citation graphs, recommendation systems, etc. Effectively learning from TAGs would enable better representations of both structural and textual representations of the graph and improve decision-making in relevant domains. We present GaLoRA, a parameter-efficient framework that integrates structural information into LLMs. GaLoRA demonstrates competitive performance on node classification tasks with TAGs, performing on par with state-of-the-art models with just 0.24% of the parameter count required by full LLM fine-tuning. We experiment with three real-world datasets to showcase GaLoRA's effectiveness in combining structural and semantical information on TAGs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GaLoRA**（Graph-aware Low-Rank Adaptation）的参数高效框架，旨在解决文本属性图（TAGs）上的节点分类问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持计算效率的同时，让大语言模型（LLMs）能够同时理解文本属性图（TAGs）中的**丰富语义信息**和**复杂的拓扑结构信息**。
*   **研究动机**：
    *   传统的图神经网络（GNNs）擅长结构建模但语义理解有限；预训练语言模型（PLMs）语义强但缺乏结构感知。
    *   现有的融合方法（如 GLEM 或 GraphAdapter）要么计算开销巨大（全量微调），要么由于冻结了 LLM 而限制了其对特定任务语义的适应能力。
    *   需要一种模块化、参数高效且能实现结构-语义深度融合的方案。

### 2. 方法论（GaLoRA 框架）
GaLoRA 采用两阶段解耦设计，核心思想是将 GNN 学习到的结构嵌入通过低秩自适应（LoRA）机制注入到 LLM 的特定层中。

*   **第一阶段：GNN 结构学习**
    *   使用 GraphSAGE 等 GNN 模型在图上进行预训练。
    *   提取两类结构嵌入：**Pass-1**（捕捉 1 步邻域信息）和 **Pass-2**（捕捉 2 步邻域信息）。
*   **第二阶段：结构感知 LLM 微调**
    *   **注入机制**：利用 LoRA 机制，在 LLM 的冻结层中加入可训练的低秩矩阵。
    *   **融合公式**：通过一个可学习的门控参数 $\alpha$ 来平衡文本隐藏状态 $H_1$ 和结构嵌入 $H_2$。公式表达为：$Z = W_C \cdot (\alpha \cdot W_A H_1 + (1 - \alpha) \cdot W_B H_2)$。其中 $W_A, W_B$ 将输入投影到低秩空间，$W_C$ 投影回原始维度。
    *   **层级策略**：将 Pass-1 嵌入注入 LLM 的**中间层**（辅助形成词间上下文），将 Pass-2 嵌入注入 **高层**（辅助进行更高层级的语义推理）。

### 3. 实验设计
*   **数据集**：
    *   **Instagram**：二分类（商业 vs 非商业用户）。
    *   **Reddit**：二分类（热门 vs 非热门用户）。
    *   **ArXiv**：40 类论文分类（使用了 4.6 万节点的子图）。
*   **Benchmark 与对比方法**：
    *   **基准**：GNN-only, LLM-only, LLM (LoRA)。
    *   **SOTA 对比**：GLEM（基于 EM 框架）、TAPE（基于提示工程）、GraphAdapter（基于适配器）。
*   **评估指标**：ROC-AUC（Instagram）和 Accuracy（Reddit, ArXiv）。

### 4. 资源与算力
*   **硬件**：使用 Google Colab 上的 **NVIDIA A100 GPU (52 GB VRAM)**。
*   **软件**：基于 PyTorch, PyTorch Geometric 和 HuggingFace Transformers。
*   **训练细节**：针对不同数据集设置了不同的 Token 长度（96 到 256 不等），使用了 AdamW 优化器。文中未明确给出总训练时长，但强调了其参数量极低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个真实数据集上进行了主实验，并使用了 5 个不同的随机种子取平均值，保证了结果的统计客观性。
    *   **消融实验**：研究了 LoRA 秩（Rank $r \in \{2, 4, 8\}$）对性能的影响。
    *   **提示工程分析**：探讨了不同 Prompt 对分类效果的提升。
    *   **模型变体**：对比了 GPT-2 和 RoBERTa 作为骨干网络的效果，以及 GAT 与 GraphSAGE 作为 GNN 模块的效果。
*   **充分性评价**：实验设计较为全面，涵盖了性能对比、参数效率分析和组件影响分析，实验过程考虑了公平性（如在相同 LLM 骨干下对比）。

### 6. 主要结论与发现
*   **高性能与高效率**：GaLoRA 在性能上与 SOTA 模型（如 GraphAdapter）持平甚至在部分数据集上有所超越。
*   **极低参数量**：GaLoRA 仅需训练 **0.295M** 个参数，仅占 GPT-2 总参数量的 **0.238%**，远低于全量微调。
*   **结构注入的有效性**：实验证明，将不同步数的图结构信息注入 LLM 的不同层级，能显著提升模型对 TAGs 的理解能力。
*   **小模型潜力**：即使是较小的语言模型（如 GPT-2），在注入结构信息后也能展现出极强的竞争力。

### 7. 优点与亮点
*   **模块化解耦**：GNN 和 LLM 分开训练，降低了联合训练的复杂度和显存压力。
*   **参数极其精简**：通过 LoRA 注入结构特征，实现了极高的参数效率，适合资源受限的场景。
*   **层级注入策略**：根据 GNN 感受野的大小有针对性地选择 LLM 注入层，符合深度学习的层级特征表示逻辑。

### 8. 不足与局限
*   **任务覆盖有限**：目前仅在**节点分类**任务上进行了验证，尚未扩展到链路预测或图分类任务。
*   **数据集规模限制**：由于算力限制，在 ArXiv 数据集上仅使用了子图进行实验，可能无法完全反映在大规模全图上的表现。
*   **潜在的信息泄露**：论文提到采用分层划分（Stratified Splits），但在 GNN 消息传递过程中可能存在一定的结构信息泄露风险。
*   **骨干模型较旧**：实验主要基于 GPT-2 和 RoBERTa，虽然证明了有效性，但未在最新的超大规模模型（如 Llama 3 等）上进行广泛测试。

（完）
