---
title: "When Fine-Tuning Fails and when it Generalises: Role of Data Diversity and Mixed Training in LLM-based TTS"
title_zh: 微调何时失败以及何时泛化：数据多样性和混合训练在基于大语言模型（LLM）的文本转语音（TTS）中的作用
authors: "Anupam Purwar, Aditya Choudhary"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10904v1"
tags: ["query:peft"]
score: 10.0
evidence: 对语言模型主干进行LoRA微调
tldr: 本研究探讨了在基于大语言模型（LLM）的文本转语音（TTS）系统中，微调对提升语音质量的作用。研究发现，冻结的 LLM 难以捕捉特定说话人的声学特征，而通过 LoRA 对 Qwen-0.5B 进行微调，能显著改善语音一致性、信噪比和感知质量。实验证明，当训练数据具备足够的声学多样性时，LoRA 不仅能增强说话人相似度，还能在不损害语言建模能力的前提下提升信号质量，为轻量化 TTS 系统的说话人自适应提供了有效路径。
selection_source: fresh_fetch
motivation: 现有的冻结 LLM 表征在 TTS 任务中难以充分建模说话人的特定声学和感知特征。
method: 采用 LoRA 技术对 Qwen-0.5B 模型进行微调，并利用量化后的 GGUF 格式实现低延迟部署。
result: "微调使 DNS-MOS 提升高达 0.42 分，信噪比增加 34%，且在所有测试说话人上均实现了更高的相似度。"
conclusion: LoRA 微调是提升紧凑型 LLM-TTS 系统说话人自适应能力的有效手段，其成功关键在于训练数据的声学多样性。
---

## 摘要
大语言模型正越来越多地被用作神经文本转语音（TTS）系统的语义骨干。然而，冻结的 LLM 表示不足以建模特定说话人的声学和感知特征。我们对 TTS 的语言模型骨干进行微调的实验表明，在语音克隆任务中，提高语音一致性和信噪比（SNR）具有前景。在多个说话人中，LoRA 微调在语音质量的三个互补维度上始终优于未微调的基础 Qwen-0.5B 模型。首先，对于训练数据表现出足够声学变异性的说话人，感知质量显著提高，DNS-MOS 增益高达 0.42 分。其次，所有评估说话人的说话人保真度均有所提高，语音相似度持续增加，这表明 LoRA 有效地适应了说话人身份表示，且没有降低语言建模能力。第三，在大多数情况下，信号级质量得到改善，信噪比增加了多达 34%。关键在于，这些改进在很大程度上受训练数据特征的支配。声学能量和感知质量具有高变异性的说话人在 DNS-MOS、语音相似度和 SNR 方面同时获得提升。总的来说，这项工作确立了 LoRA 微调不仅是一种参数高效的优化技术，而且是在紧凑型基于 LLM 的 TTS 系统中实现更好说话人级别自适应的有效机制。在足够多样化的训练数据支持下，经过 LoRA 适配的 Qwen-0.5B 在感知质量和说话人相似度方面始终超越其冻结的基础模型，并能通过量化形式托管的 GGUF 模型实现低延迟。

## Abstract
Large language models are increasingly adopted as semantic backbones for neural text-to-speech systems. However, frozen LLM representations are insufficient for modeling speaker specific acoustic and perceptual characteristics. Our experiments involving fine tuning of the Language Model backbone of TTS show promise in improving the voice consistency and Signal to Noise ratio SNR in voice cloning task. Across multiple speakers LoRA finetuning consistently outperforms the non-finetuned base Qwen-0.5B model across three complementary dimensions of speech quality. First, perceptual quality improves significantly with DNS-MOS gains of up to 0.42 points for speakers whose training data exhibits sufficient acoustic variability. Second, speaker fidelity improves for all evaluated speakers with consistent increases in voice similarity indicating that LoRA effectively adapts speaker identity representations without degrading linguistic modeling. Third, signal level quality improves in most cases with signal to noise ratio increasing by as much as 34 percent. Crucially these improvements are strongly governed by the characteristics of the training data. Speakers with high variability in acoustic energy and perceptual quality achieve simultaneous gains in DNS-MOS voice similarity and SNR. Overall this work establishes that LoRA finetuning is not merely a parameter efficient optimization technique but an effective mechanism for better speaker level adaptation in compact LLM-based TTS systems. When supported by sufficiently diverse training data LoRA adapted Qwen-0.5B consistently surpasses its frozen base model in perceptual quality speaker similarity with low latency using GGUF model hosted in quantized form.

---

## 论文详细总结（自动生成）

这篇论文深入探讨了在基于大语言模型（LLM）的文本转语音（TTS）系统中，对语言模型主干进行微调的影响因素，特别是数据多样性和混合训练的作用。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 LLM 的 TTS 系统通常冻结其语言模型主干，仅训练下游的声学解码器。然而，冻结的 LLM 表征难以充分捕捉特定说话人的声学细节和感知特征。
*   **研究动机**：作者旨在探索通过参数高效微调（LoRA）直接调整 LLM 主干（如 Qwen-0.5B），是否能提升语音克隆的相似度、感知质量（MOS）和信号质量（SNR），并研究训练数据的特性如何影响微调的效果。

### 2. 核心方法论
*   **核心思想**：将 LoRA 应用于 LLM 主干的注意力层，使其从单纯的文本/语义建模转向更具说话人特征的声学标记（Acoustic Token）预测。
*   **关键技术细节**：
    *   **模型架构**：以 Qwen-2.5-0.5B 为语义骨干，结合神经编解码器（Neural Codec）进行语音合成。
    *   **LoRA 配置**：在注意力层的 `q_proj`、`k_proj`、`v_proj` 上应用 LoRA，秩（Rank）设为 8，Alpha 设为 16。
    *   **量化与部署**：使用 GGUF 格式对模型进行 8 位（Q8）量化，以降低推理延迟并支持边缘端部署。
    *   **推理控制**：通过调整采样温度（Temperature）和 Top-k 采样来平衡语音的自然度与稳定性。

### 3. 实验设计
*   **数据集**：
    *   **HiFi TTS**：包含高质量的朗读音频（说话人 ID: 1, 2, 11614）。
    *   **Libriheavy-HQ**：包含更长、声学环境更复杂的音频（说话人 ID: 1401, 1212, 1259）。
*   **Benchmark（基准）**：未微调的原始 Qwen-0.5B 基础模型。
*   **对比方法**：
    *   **Pure FT**：针对单个说话人的专用微调。
    *   **Mixed FT**：多说话人混合训练（如 2+2+2 小时或 1+1+1 小时配置）。
    *   **Zero-shot**：在未见过的说话人上测试混合训练模型的泛化能力。
*   **评估指标**：DNS-MOS（感知质量）、WADA-SNR（信噪比）、WeSpeaker 嵌入余弦相似度（说话人相似度）以及推理延迟。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA L4 GPU (24GB VRAM)**。
*   **训练参数**：
    *   **全量微调**：Batch Size 为 2，使用 Adam 优化器。
    *   **LoRA 微调**：Batch Size 为 4，梯度累加为 2（等效 Batch Size 为 8）。
    *   **时长**：所有实验均运行 5 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个具有不同声学特性的说话人，进行了单说话人微调、多说话人混合微调、零样本泛化测试以及量化消融实验。
*   **充分性评价**：实验设计较为全面，不仅关注最终指标，还深入分析了训练过程中的损失函数与感知质量的关系。通过对比不同数据量和数据多样性，验证了结论的普适性。实验过程客观，考虑了推理超参数对结果的影响。

### 6. 主要结论与发现
*   **数据多样性是关键**：训练数据的声学能量变异性（Energy Std > 13 dB）与微调增益呈正相关。数据过于单一（同质化）会导致微调效果不佳甚至感知质量下降。
*   **损失-质量背离现象**：在低变异性数据上，训练/验证损失可能持续下降，但 DNS-MOS 却在恶化。这意味着损失函数不能作为 LLM-TTS 微调停止的唯一指标。
*   **LoRA 的优越性**：LoRA 微调在提升说话人相似度方面非常一致，且在高质量数据支持下，DNS-MOS 提升可达 0.42 分，SNR 提升可达 34%。
*   **混合训练的泛化力**：在 HiFi TTS 上训练的多说话人模型，在 Libriheavy 说话人上表现出良好的零样本泛化能力，甚至优于针对该说话人的专用微调模型（当该说话人数据质量较低时）。
*   **延迟优化**：GGUF Q8 量化使推理速度提升了 4.5 到 6.9 倍，且 LoRA 适配器带来的额外开销极小。

### 7. 优点
*   **深入的失效分析**：不仅展示了微调成功的情况，还详细分析了“微调何时失败”，提出了数据多样性这一核心影响因素。
*   **实用性强**：结合了量化技术（GGUF），证明了微调后的轻量级模型（0.5B）在实际生产环境中的可行性。
*   **发现新现象**：识别并命名了“损失-质量背离”现象，对未来 TTS 模型的训练具有指导意义。

### 8. 不足与局限
*   **模型规模限制**：研究仅限于 0.5B 的小规模 LLM，结论是否适用于 7B 或更大规模的模型尚待验证。
*   **数据利用不完全**：仅使用了部分说话人的前 5000 个样本，未探索更大规模数据量下的性能极限。
*   **声学分析较初步**：对音频数据的分析主要集中在频率和能量分布，缺乏更深层次的频谱特征分析。
*   **任务局限性**：研究结果主要针对 TTS 任务，可能无法直接推广到 LLM 的文本生成等其他任务。

（完）
