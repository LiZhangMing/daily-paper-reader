---
title: "Med-DualLoRA: Local Adaptation of Foundation Models for 3D Cardiac MRI"
title_zh: Med-DualLoRA：用于 3D 心脏磁共振成像基础模型的本地适配
authors: "Joan Perramon-Llussà, Amelia Jiménez-Sánchez, Grzegorz Skorupko, Fotis Avgoustidis, Carlos Martín-Isla, Karim Lekadir, Polyxeni Gkontra"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10967v1"
tags: ["query:peft"]
score: 10.0
evidence: 用于客户端感知参数高效微调的Med-DualLoRA
tldr: 针对3D心脏MRI基础模型在多中心联邦学习中面临的数据异构和通信开销问题，本文提出Med-DualLoRA框架。该方法通过加性分解将LoRA分为全局共享和本地私有模块，仅同步全局参数以保护隐私并降低成本。实验证明，该方法在保持通信效率的同时，显著提升了疾病检测的准确性，为医疗基础模型的本地化适配提供了可扩展方案。
selection_source: fresh_fetch
motivation: 传统的医疗基础模型微调面临单中心数据偏差、隐私限制以及联邦学习中的非独立同分布数据和高通信成本挑战。
method: 提出Med-DualLoRA框架，利用加性分解将LoRA拆分为全局共享和本地私有组件，并仅对部分Transformer块进行微调以提高效率。
result: "在ACDC和M&Ms数据集上的实验显示，该方法达到了0.768的平衡准确率，在性能和通信效率上均优于现有的联邦参数高效微调基准。"
conclusion: Med-DualLoRA为临床约束下的医疗基础模型联邦适配提供了一种高效、隐私保护且具备个性化能力的解决方案。
---

## 摘要
基础模型 (FMs) 在经过特定任务适配后，在包括心脏磁共振 (CMR) 在内的各种医学成像任务和模态中展现出强大的下游性能潜力。然而，使用单站点数据进行适配可能会导致性能欠佳并增加模型偏差，而由于隐私限制，对临床数据进行集中式微调通常是不可行的。联邦微调提供了一种保护隐私的替代方案；然而，传统方法在异构、非独立同分布 (non-IID) 的多中心数据下表现挣扎，且在适配大型模型时会产生巨大的通信开销。在这项工作中，我们研究了用于 3D CMR 疾病检测的联邦基础模型微调，并提出了 Med-DualLoRA，这是一种客户端感知的参数高效微调 (PEFT) 联邦框架，通过加法分解将全局共享和本地低秩适配 (LoRA) 解耦。全局和本地 LoRA 模块在本地进行训练，但只有全局组件在站点间共享和聚合，从而保持本地适配器的私密性。这种设计在显著降低通信成本的同时提高了个性化程度，实验表明，仅适配两个 Transformer 块即可在保持性能的同时进一步提高效率。我们在 ACDC 和组合的 M&Ms 数据集上，使用针对疾病检测进行微调的多中心最先进电影 (cine) 3D CMR 基础模型评估了我们的方法，并将每个厂商视为一个联邦客户端。与其他联邦 PEFT 基准相比，Med-DualLoRA 实现了具有统计学意义的性能提升（平衡准确率 0.768，特异性 0.612），同时保持了通信效率。我们的方法为现实临床约束下医学基础模型的本地联邦适配提供了一种可扩展的解决方案。

## Abstract
Foundation models (FMs) show great promise for robust downstream performance across medical imaging tasks and modalities, including cardiac magnetic resonance (CMR), following task-specific adaptation. However, adaptation using single-site data may lead to suboptimal performance and increased model bias, while centralized fine-tuning on clinical data is often infeasible due to privacy constraints. Federated fine-tuning offers a privacy-preserving alternative; yet conventional approaches struggle under heterogeneous, non-IID multi-center data and incur substantial communication overhead when adapting large models. In this work, we study federated FM fine-tuning for 3D CMR disease detection and propose Med-DualLoRA, a client-aware parameter-efficient fine-tuning (PEFT) federated framework that disentangles globally shared and local low-rank adaptations (LoRA) through additive decomposition. Global and local LoRA modules are trained locally, but only the global component is shared and aggregated across sites, keeping local adapters private. This design improves personalization while significantly reducing communication cost, and experiments show that adapting only two transformer blocks preserves performance while further improving efficiency. We evaluate our method on a multi-center state-of-the-art cine 3D CMR FM fine-tuned for disease detection using ACDC and combined M\&Ms datasets, treating each vendor as a federated client. Med-DualLoRA achieves statistically significant improved performance (balanced accuracy 0.768, specificity 0.612) compared to other federated PEFT baselines, while maintaining communication efficiency. Our approach provides a scalable solution for local federated adaptation of medical FMs under realistic clinical constraints.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Med-DualLoRA**，一种专门为 3D 心脏磁共振成像（CMR）基础模型设计的联邦学习微调框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：基础模型（Foundation Models, FMs）在医学影像任务中表现出强大的迁移能力，但针对特定临床场景的适配面临挑战。
*   **核心问题**：
    1.  **隐私限制**：医疗数据通常分布在不同机构，由于隐私政策无法集中训练。
    2.  **数据异构性（Non-IID）**：不同医院使用不同的扫描仪（厂商如 Siemens, GE 等）和协议，导致标准联邦学习效果不佳。
    3.  **通信开销**：基础模型参数量巨大，在联邦学习中频繁传输全量参数或大型更新会导致极高的网络负担。
*   **研究意义**：提出一种既能保护隐私、又能应对数据异构，且通信效率高的本地适配方案。

### 2. 论文提出的方法论：Med-DualLoRA
*   **核心思想**：通过**加性分解（Additive Decomposition）**将低秩适配器（LoRA）拆分为“全局共享”和“本地私有”两个部分。
*   **关键技术细节**：
    *   **双重 LoRA 结构**：在冻结的基础模型（Backbone）层中注入两个并行的 LoRA 模块。
        *   **全局 LoRA ($A_g, B_g$)**：学习跨中心的通用特征表示，参与联邦聚合。
        *   **本地 LoRA ($A_{li}, B_{li}$)**：学习特定站点的个性化特征（如特定扫描仪的伪影或分布），始终留在本地，不参与共享。
    *   **前向传播公式**：$f_i(x) = Wx + \frac{\alpha}{r} B_g A_g x + \frac{\alpha}{r} B_{li} A_{li} x$。其中 $W$ 是冻结的预训练权重。
    *   **算法流程**：客户端在本地同时训练全局和本地适配器；通信时，仅将全局 LoRA 的参数上传至服务器进行加权平均（FedAvg），然后下发更新。

### 3. 实验设计
*   **数据集**：
    *   **ACDC**：150 名患者的受控环境数据。
    *   **M&Ms (1 & 2)**：543 名患者，涵盖 5 个机构和 4 种厂商（Siemens, GE, Philips, Canon），具有高度的临床变异性。
*   **任务**：基于 3D CMR 影像的二分类疾病检测。
*   **对比基准（Benchmarks）**：
    *   **集中式训练**：全量微调（Full-ft）、仅头部微调（Head-only）、标准 LoRA。
    *   **联邦学习基准**：Federated Head-only、Federated LoRA。
    *   **本地基准**：仅使用本地数据训练的客户端模型。
*   **主干模型**：采用最先进的电影心脏磁共振基础模型 **CineMA**。

### 4. 资源与算力
*   **硬件设备**：
    *   **集中式实验**：单张 NVIDIA GeForce RTX 4090 GPU。
    *   **联邦学习实验**：双 NVIDIA L40S 系统，用于模拟多个客户端的并发更新。
*   **训练细节**：集中式模型训练最多 500 轮（带早停）；联邦学习中每个通信轮次客户端执行 1 个本地 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个不同的客户端（4 个厂商 + ACDC 数据集）。
    *   所有实验均运行 3 个随机种子并取平均值，进行了统计显著性检验（配对 t 检验）。
    *   **消融实验**：针对适配的 Transformer 模块数量（从 2 个到 12 个全量块）进行了帕累托前沿（Pareto frontier）分析，探讨通信成本与准确率的权衡。
*   **评价**：实验设计客观、公平，充分考虑了现实中医疗数据的类别不平衡和设备异构性问题。

### 6. 主要结论与发现
*   **性能优越**：Med-DualLoRA 达到了 **0.768** 的平衡准确率，显著优于联邦 LoRA（0.693）和 Head-only（0.514）。
*   **弥合差距**：该方法成功缩小了联邦学习与集中式训练之间的性能差距，且在特异性（Specificity）上有大幅提升。
*   **高效通信**：实验发现，**仅适配 2 个 Transformer 块**即可达到接近最优的性能，每轮通信量仅约 28 MB，远低于全量适配。
*   **鲁棒性**：通过解耦本地特征，模型在小型数据集（如 Canon 厂商）上的表现更加稳健。

### 7. 优点
*   **结构创新**：首次在 3D 医疗影像基础模型的联邦适配中引入了显式的全局/本地结构分离。
*   **隐私增强**：本地适配器不共享，进一步降低了通过模型参数逆向推导原始数据的风险。
*   **实用性强**：针对通信带宽受限的临床环境，提供了明确的参数量与性能权衡建议。

### 8. 不足与局限
*   **聚合策略单一**：目前仅使用了基础的 FedAvg 聚合算法，未结合 FedProx 或 Scaffold 等更先进的抗非独立同分布算法。
*   **任务覆盖有限**：实验仅集中在疾病检测（分类任务），尚未在分割或配准等其他 CMR 任务上验证。
*   **数据划分风险**：沿用了原始数据集的划分，这可能导致某些客户端内部的类别极度不平衡，未来可尝试分层划分以进一步验证稳定性。

（完）
