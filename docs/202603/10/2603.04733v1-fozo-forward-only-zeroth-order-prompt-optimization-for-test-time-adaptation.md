---
title: "FOZO: Forward-Only Zeroth-Order Prompt Optimization for Test-Time Adaptation"
title_zh: FOZO：用于测试时自适应的仅前向零阶提示优化
authors: "Xingyu Wang, Tao Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04733v1"
tags: ["query:peft"]
score: 9.0
evidence: 内存高效的零阶提示优化
tldr: 针对测试时自适应（TTA）中反向传播计算开销大且修改模型权重的问题，本文提出FOZO框架。FOZO采用仅前向传播的零阶优化方法，通过优化中间特征统计量和预测熵来调整提示词（Prompt）。该方法引入动态衰减扰动尺度以保证稳定性，在多个ImageNet变体数据集上超越了主流梯度算法及SOTA前向算法，且适用于资源受限的量化模型部署。
selection_source: fresh_fetch
motivation: 现有的TTA方法要么因反向传播导致计算资源消耗过高，要么因无需反向传播而导致自适应能力受限。
method: 提出一种基于零阶梯度的提示词优化方法，结合中间特征统计与预测熵目标，并引入动态衰减的扰动尺度以确保收敛。
result: "在ImageNet-C等数据集上取得了59.52%的Top-1准确率，优于主流梯度方法及最先进的前向优化算法FOA。"
conclusion: FOZO为资源受限场景下的测试时自适应提供了一种高效、稳定且极具竞争力的无反向传播解决方案。
---

## 摘要
测试时自适应 (TTA) 对于使深度学习模型能够处理现实世界的数据分布偏移至关重要。然而，当前的方法面临显著的局限性：基于反向传播的方法由于其高计算和内存需求，以及在自适应过程中倾向于修改模型权重，不适用于低端部署设备；而传统的无反向传播技术则表现出受限的自适应能力。在这项工作中，我们提出了仅前向零阶优化 (FOZO)，这是一种用于 TTA 的新型且实用的无反向传播范式。FOZO 利用内存高效的零阶提示优化，由优化中间特征统计量和预测熵的目标所引导。为了确保在分布外数据流上的高效且稳定的自适应，我们在零阶梯度估计过程中引入了动态衰减的扰动尺度，并在理论上证明了其在 TTA 数据流假设下的收敛性。在 ImageNet-C、ImageNet-R 和 ImageNet-Sketch 上进行的广泛持续自适应实验证明了 FOZO 的卓越性能，在 ImageNet-C (5K, level 5) 上实现了 59.52% 的 Top-1 准确率，优于主要的基于梯度的方法和最先进的 (SOTA) 仅前向方法 FOA (58.13%)。此外，FOZO 在量化 (INT8) 模型上表现出强大的泛化能力。这些发现表明，FOZO 是资源受限场景下 TTA 部署的一个极具竞争力的解决方案。

## Abstract
Test-Time Adaptation (TTA) is essential for enabling deep learning models to handle real-world data distribution shifts. However, current approaches face significant limitations: backpropagation-based methods are not suitable for low-end deployment devices, due to their high computation and memory requirements, as well as their tendency to modify model weights during adaptation; while traditional backpropagation-free techniques exhibit constrained adaptation capabilities. In this work, we propose Forward-Only Zeroth-Order Optimization (FOZO), a novel and practical backpropagation-free paradigm for TTA. FOZO leverages a memory-efficient zeroth-order prompt optimization, which is led by objectives optimizing both intermediate feature statistics and prediction entropy. To ensure efficient and stable adaptation over the out-of-distribution data stream, we introduce a dynamically decaying perturbation scale during zeroth-order gradient estimation and theoretically prove its convergence under the TTA data stream assumption. Extensive continual adaptation experiments on ImageNet-C, ImageNet-R, and ImageNet-Sketch demonstrate FOZO's superior performance, achieving 59.52% Top-1 accuracy on ImageNet-C (5K, level 5) and outperforming main gradient-based methods and SOTA forward-only FOA (58.13%). Furthermore, FOZO exhibits strong generalization on quantized (INT8) models. These findings demonstrate that FOZO is a highly competitive solution for TTA deployment in resource-limited scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《FOZO: Forward-Only Zeroth-Order Prompt Optimization for Test-Time Adaptation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**测试时自适应（Test-Time Adaptation, TTA）**。在实际部署中，预训练模型常面临测试数据与训练数据分布不一致（分布偏移）的问题。
*   **现有挑战**：
    *   **计算资源限制**：基于反向传播（BP）的方法显存和计算开销巨大，不适合边缘设备。
    *   **模型权重不可变**：许多部署场景要求模型权重冻结（如硬编码或量化模型），无法进行梯度更新。
    *   **现有前向方法不足**：如 FOA 使用进化策略（CMA-ES）优化提示词（Prompt），但在高维空间下收敛极慢且性能受限。
*   **核心目标**：提出一种**仅需前向传播（Forward-Only）**、内存高效且收敛快速的零阶优化框架，在不修改模型权重的前提下实现高效自适应。

### 2. 论文提出的方法论：FOZO
FOZO（Forward-Only Zeroth-Order Optimization）的核心思想是利用零阶梯度估计来优化输入层的可学习提示词（Visual Prompts）。
*   **零阶梯度估计（SPSA）**：通过在提示词空间施加随机扰动（正向和负向），利用两次前向传播的损失差值来估计梯度方向，从而避开复杂的反向传播。
*   **动态扰动方案（Dynamic Perturbation）**：
    *   引入可变扰动尺度 $\epsilon$。在自适应初期或检测到新的领域偏移（损失波动）时，增大 $\epsilon$ 以增强探索能力，帮助跳出局部最优；
    *   随着优化稳定，$\epsilon$ 按比例衰减，以确保在平滑区域精确收敛。
*   **损失函数设计**：
    *   **预测熵最小化**：鼓励模型产生确定的预测。
    *   **深浅层特征统计对齐**：将模型中间层（分为深层和浅层两组）的 [CLS] 标记激活统计量（均值和方差）与源域统计量进行对齐，增强特征稳定性。
*   **理论支撑**：论文在理论上证明了该方法在 TTA 数据流假设下的收敛性，指出其收敛速度取决于 Hessian 矩阵的有效秩（Effective Rank）而非参数的总维度。

### 3. 实验设计
*   **数据集**：ImageNet-C（包含 15 种损坏类型）、ImageNet-R（艺术/渲染图）、ImageNet-Sketch（草图）。
*   **实验场景**：
    *   **持续自适应（Continual TTA）**：模拟真实世界中数据流不断变化且领域边界未知的场景。
    *   **量化模型自适应**：在 8-bit 整数（INT8）量化模型上测试。
    *   **混合偏移（Mixed Shifts）**：多种偏移类型交替出现的极端情况。
*   **Benchmark 与对比方法**：
    *   **前向方法**：LAME, T3A, FOA, ZOA。
    *   **反向传播方法**：TENT, EATA, SAR, DeYO。
    *   **模型架构**：以 ViT-Base 为主。

### 4. 资源与算力
*   **硬件平台**：实验主要在 **NVIDIA RTX 4090 GPU** 上进行。
*   **性能指标**：论文详细记录了处理 ImageNet-C (5k) 数据集的总运行时间（秒）和峰值显存占用（MiB）。
*   **算力效率**：FOZO 在显存占用上远低于基于 BP 的方法（约 831 MiB vs 5500 MiB），且在达到相同准确率时，运行时间仅为 FOA 和 ZOA 的 66% 左右。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个大型数据集、15 种损坏类型、5 个严重等级。
*   **消融实验**：对动态扰动、特征对齐损失、提示词数量、Batch Size 等关键组件和超参数进行了详尽的消融分析。
*   **客观性与公平性**：
    *   在相同的前向传播次数（FP=2, 26, 28）下与 SOTA 方法对比。
    *   使用了 RobustBench 推荐的 5k 采样设置，确保了评估的标准化。
    *   实验覆盖了从全精度到量化模型的多种部署形态，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：FOZO 在 ImageNet-C 上达到了 59.52% 的 Top-1 准确率，显著优于 FOA (58.13%)，甚至在某些指标上超过了基于梯度的 TENT。
*   **收敛极快**：动态扰动策略显著加速了收敛过程，使其在处理在线数据流时更具优势。
*   **量化友好**：在 INT8 量化模型上，FOZO 依然能保持稳定的提升，证明了其在边缘设备部署的潜力。
*   **参数高效**：仅需更新极少量的提示词参数（约 2304 个），远少于更新 LayerNorm 参数的方法。

### 7. 优点
*   **部署便捷**：无需计算图和反向传播，极大地降低了显存门槛。
*   **理论完备**：不仅有实验支撑，还提供了严谨的收敛性证明。
*   **鲁棒性强**：动态扰动机制有效解决了零阶优化在非平稳数据流中容易陷入局部最优的问题。
*   **通用性**：适用于权重冻结或量化的黑盒模型。

### 8. 不足与局限
*   **前向开销**：虽然避开了 BP，但为了获得准确的梯度估计，通常需要多次前向传播（如 26-28 次），在对单次推理延迟极其敏感的场景下可能仍显沉重。
*   **架构依赖**：目前主要针对 Vision Transformer (ViT) 架构进行验证，在 CNN 等其他架构上的表现尚未充分探讨。
*   **超参数敏感性**：动态衰减因子 $\alpha$ 和阈值 $\tau$ 等参数可能需要根据不同任务进行微调。

（完）
