---
title: "LoRA-MME: Multi-Model Ensemble of LoRA-Tuned Encoders for Code Comment Classification"
title_zh: LoRA-MME：用于代码注释分类的 LoRA 微调编码器多模型集成
authors: "Md Akib Haider, Ahsan Bulbul, Nafis Fuad Shahid, Aimaan Ahmed, Mohammad Ishrak Abedin"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03959v2"
tags: ["query:peft"]
score: 10.0
evidence: 利用参数高效微调(PEFT)和LoRA的多模型集成架构
tldr: "本研究针对NLBSE'26竞赛中的代码注释分类任务，提出了LoRA-MME架构。该方法利用参数高效微调（PEFT）技术，集成了UniXcoder、CodeBERT、GraphCodeBERT和CodeBERTa四种编码器。通过对各模型进行LoRA独立微调并采用加权集成策略，在不增加全量微调内存开销的情况下，显著提升了Java、Python和Pharo语言的多标签分类性能，实现了语义准确性与资源消耗的平衡。"
selection_source: fresh_fetch
motivation: 旨在解决多语言代码注释分类中的多标签挑战，并降低全量微调带来的高昂内存开销。
method: 利用LoRA技术对四种主流代码Transformer编码器进行参数高效微调，并结合学习到的加权集成策略进行预测汇总。
result: "在测试集上获得了0.7906的加权F1分数，但受限于集成模型的计算成本，最终竞赛综合评分为41.20%。"
conclusion: 多模型集成策略能有效提升代码注释分类的准确率，但在实际应用中需权衡推理效率与语义精度之间的关系。
---

## 摘要
代码注释分类是自动化软件文档编写和分析中的一项关键任务。在 NLBSE'26 工具竞赛的背景下，我们提出了 LoRA-MME，这是一种利用参数高效微调（PEFT）的多模型集成架构。我们的方法通过结合四种不同的 Transformer 编码器（UniXcoder、CodeBERT、GraphCodeBERT 和 CodeBERTa）的优势，解决了 Java、Python 和 Pharo 跨语言的多标签分类挑战。通过使用低秩自适应（LoRA）独立微调这些模型，并通过学习到的加权集成策略聚合其预测结果，我们在没有全模型微调内存开销的情况下，最大限度地提高了分类性能。我们的工具在测试集上取得了 0.7906 的加权 F1 分数和 0.6867 的宏 F1 分数。然而，集成的计算成本导致最终提交分数为 41.20%，这突显了语义准确性与推理效率之间的权衡。

## Abstract
Code comment classification is a critical task for automated software documentation and analysis. In the context of the NLBSE'26 Tool Competition, we present LoRA-MME, a Multi-Model Ensemble architecture utilizing Parameter-Efficient Fine-Tuning (PEFT). Our approach addresses the multi-label classification challenge across Java, Python, and Pharo by combining the strengths of four distinct transformer encoders: UniXcoder, CodeBERT, GraphCodeBERT, and CodeBERTa. By independently fine-tuning these models using Low-Rank Adaptation(LoRA) and aggregating their predictions via a learned weighted ensemble strategy, we maximize classification performance without the memory overhead of full model fine-tuning. Our tool achieved an F1 Weighted score of 0.7906 and a Macro F1 of 0.6867 on the test set. However, the computational cost of the ensemble resulted in a final submission score of 41.20%, highlighting the trade-off between semantic accuracy and inference efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《LoRA-MME: Multi-Model Ensemble of LoRA-Tuned Encoders for Code Comment Classification》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效且准确地对多语言（Java, Python, Pharo）源代码注释进行多标签语义分类（如总结、用法、弃用警告等）。
*   **研究背景**：代码注释对软件维护至关重要，但自动分类面临挑战。传统方法（如手工特征或通用 Sentence-BERT）难以捕捉代码特有的语义（如 API 引用、语法结构）。
*   **研究动机**：全量微调多个大型 Transformer 模型会带来巨大的内存开销。本文旨在利用**参数高效微调（PEFT）**技术，在有限算力下集成多个代码专用编码器的优势，提升分类精度。

### 2. 方法论
*   **核心思想**：结合四种预训练代码编码器的多样性，通过 **LoRA（低秩自适应）** 进行轻量化微调，并采用**学习型加权集成**和**分类阈值优化**策略。
*   **关键技术细节**：
    *   **基础模型集成**：选择了 UniXcoder（跨模态）、CodeBERT（语义对齐）、GraphCodeBERT（数据流结构）和 CodeBERTa（轻量级）四种模型。
    *   **LoRA 配置**：在注意力机制的 Query、Key、Value 和 Dense 层注入 LoRA 适配器。设置秩 $r=16$，$\alpha=32$。每模型仅需训练约 5.9M 参数（约占总参数的 4.5%）。
    *   **学习型加权集成**：并非简单的概率平均，而是为每个类别 $c$ 学习一组权重 $W_c$，根据不同模型在特定类别上的表现动态分配权重。
    *   **数据预处理**：针对不同语言进行文本清洗（如修复 Pharo 中的字符错误）和特定标签（如 JavaDoc, Sphinx）的保留。
    *   **损失函数**：使用 Focal Loss 处理类别不平衡问题。
    *   **阈值优化**：针对每个“语言-类别”对进行网格搜索（0.1 到 0.9），寻找最优 F1 分数的决策阈值。

### 3. 实验设计
*   **数据集**：NLBSE'26 竞赛数据集，包含来自 20 个开源项目的 9,361 个句子（Java 6,595 条，Python 1,658 条，Pharo 1,108 条）。
*   **任务类型**：多标签分类（涉及 487 个多标签实例）。
*   **Benchmark（基准）**：竞赛官方提供的基于 SetFit（对比学习）的基准模型。
*   **对比维度**：评估了各语言下的 Macro F1 和 Weighted F1 分数。

### 4. 资源与算力
*   **训练算力**：明确提到使用 **NVIDIA RTX 3090** 显卡进行微调。
*   **推理算力**：为了竞赛公平性，推理性能在 **Google Colab T4** 标准实例上测定。
*   **效率指标**：平均推理延迟为 45.13 ms/样本，总计算量约为 235,759.28 GFLOPS。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对四种模型进行了独立微调与集成实验。
    *   进行了**阈值优化消融实验**，证明该策略使 Macro F1 提升了 0.0355。
    *   提供了详细的各语言、各类别（共 18 个类别）的精确率、召回率和 F1 分数。
*   **充分性评价**：实验设计较为客观，涵盖了多语言对比和消融分析。但由于是竞赛论文，未深入探讨不同 LoRA 超参数（如不同 $r$ 值）对结果的影响。

### 6. 主要结论与发现
*   **性能优异**：LoRA-MME 在测试集上取得了 **0.7906 的加权 F1** 和 **0.6867 的宏 F1**，显著优于基准模型（尤其在 Python 和 Pharo 上分别提升了 4.76% 和 5.16%）。
*   **精度与效率的权衡**：虽然语义分类精度高，但由于集成了四个模型，计算成本（GFLOPS）较高，导致竞赛最终综合得分仅为 41.20%。
*   **模型贡献差异**：实验发现不同模型对不同类别的贡献不同，例如 UniXcoder 在 Pharo 的示例类别中表现突出。

### 7. 优点
*   **参数高效**：利用 LoRA 技术，在不牺牲精度的情况下大幅降低了多模型集成的显存门槛。
*   **策略全面**：结合了数据清洗、Focal Loss、加权集成和逐类别阈值优化，是一套完整的工业级分类优化方案。
*   **针对性强**：充分考虑了代码注释的结构特征（如 AST、数据流）。

### 8. 不足与局限
*   **推理开销大**：集成四个 Transformer 编码器导致推理速度慢、算力消耗大，不适合对实时性要求极高的场景。
*   **复杂度高**：维护四个独立模型及一套复杂的加权逻辑增加了部署难度。
*   **改进方向单一**：作者在结论中也提到，未来需要通过**知识蒸馏**将集成模型的知识压缩到单个模型中，以解决效率瓶颈。

（完）
