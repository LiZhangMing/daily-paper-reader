---
title: "Trade-offs in Ensembling, Merging and Routing Among Parameter-Efficient Experts"
title_zh: 参数高效专家在集成、合并与路由中的权衡
authors: "Sanae Lotfi, Lucas Caccia, Alessandro Sordoni, Jordan T. Ash, Miroslav Dudik"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03535v1"
tags: ["query:peft"]
score: 10.0
evidence: 评估参数高效专家/适配器在集成、合并和路由中的权衡。
tldr: 本研究探讨了在多任务学习中融合参数高效专家模型的策略，对比了集成（Ensembling）、合并（Merging）和路由（Routing）三种主要方法。通过实证评估，研究揭示了非均匀融合策略优于均匀策略，且路由机制虽然复杂但性能提升最显著。此外，论文提出了聚类和贪婪子集选择等技术，有效降低了路由的计算开销，为模型融合提供了重要见解。
selection_source: fresh_fetch
motivation: 旨在探究在多任务学习场景下，不同模型融合策略（集成、合并、路由）的性能权衡及复杂性与收益的关系。
method: 系统性地对比了均匀与非均匀的集成与合并方法，并评估了基于输入依赖的路由机制及其专家选择优化技术。
result: 实验表明非均匀融合优于均匀融合，而路由机制在性能上表现最佳，且其计算成本可通过聚类等选择技术得到有效控制。
conclusion: 路由是融合多任务专家的最有效方式，且通过合理的专家选择策略可以在性能与计算开销之间取得良好平衡。
---

## 摘要
虽然使用轻量级适配器微调的大语言模型（LLMs）在各种任务中表现出色，但它们在单个任务上的性能取决于微调策略。融合具有不同优势的独立训练模型在多任务学习中展现出潜力，主要通过三种策略实现：集成（组合独立模型的输出）、合并（通过参数平均融合模型权重）以及路由（以输入依赖的方式整合模型）。然而，这些方法中的许多设计决策仍未得到充分研究，且更复杂的集成、合并和路由技术的相对优势也未被完全理解。我们对它们的权衡进行了实证评估，解决了两个关键问题：超越均匀集成或合并的优势是什么？路由的灵活性是否足以证明其复杂性的合理性？我们的研究结果表明，非均匀集成和合并提高了性能，但路由带来了更大的收益。为了降低路由的计算成本，我们分析了专家选择技术，结果表明聚类和贪婪子集选择可以在极小的开销下保持合理的性能。这些见解增进了我们对多任务学习中模型融合的理解。

## Abstract
While large language models (LLMs) fine-tuned with lightweight adapters achieve strong performance across diverse tasks, their performance on individual tasks depends on the fine-tuning strategy. Fusing independently trained models with different strengths has shown promise for multi-task learning through three main strategies: ensembling, which combines outputs from independent models; merging, which fuses model weights via parameter averaging; and routing, which integrates models in an input-dependent fashion. However, many design decisions in these approaches remain understudied, and the relative benefits of more sophisticated ensembling, merging and routing techniques are not fully understood. We empirically evaluate their trade-offs, addressing two key questions: What are the advantages of going beyond uniform ensembling or merging? And does the flexibility of routing justify its complexity? Our findings indicate that non-uniform ensembling and merging improve performance, but routing offers even greater gains. To mitigate the computational cost of routing, we analyze expert selection techniques, showing that clustering and greedy subset selection can maintain reasonable performance with minimal overhead. These insights advance our understanding of model fusion for multi-task learning.

---

## 论文详细总结（自动生成）

以下是对论文《Trade-offs in Ensembling, Merging and Routing Among Parameter-Efficient Experts》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着 LoRA 等参数高效微调（PEFT）技术的普及，HuggingFace 等平台上积累了大量针对不同任务微调的“专家”模型。
*   **核心问题**：在**任务标识符（Task ID）未知**的推理场景下，如何最优地整合这些独立训练的专家模型，以实现强大的多任务泛化能力？
*   **研究意义**：论文系统性地评估了三种主流融合范式——**集成（Ensembling）**、**合并（Merging）**和**路由（Routing）**之间的性能与计算成本权衡，旨在为多任务学习提供设计指南。

### 2. 方法论
论文对比了以下三种核心技术路径：
*   **集成 (Ensembling)**：在输出概率层面组合。
    *   *均匀集成*：对所有专家的预测概率取简单平均。
    *   *学习集成 (SGD)*：通过随机梯度下降优化每个专家的固定权重系数 $\lambda_i$。
    *   *蒸馏*：将复杂的集成模型预测结果蒸馏到一个单一的 LoRA 适配器中，以降低推理成本。
*   **合并 (Merging)**：在参数空间层面组合。
    *   *低秩合并*：直接对 LoRA 的 $A$ 和 $B$ 矩阵进行加权平均。
    *   *优化合并*：通过 SGD 学习全局或逐层的合并系数。研究探讨了多任务场景下的“模式连通性”（Mode Connectivity）。
*   **路由 (Routing)**：输入依赖的参数融合。
    *   *动态权重*：融合系数 $\lambda_i(x)$ 随输入 $x$ 动态变化。
    *   *专家重构*：利用模型驱动聚类（MBC）将 256 个私有专家压缩为 10 个聚类专家；或采用层次聚类（HC）在不重新训练的情况下整合专家库。

### 3. 实验设计
*   **基础模型与专家库**：使用 **Phi-2 (2.8B)** 作为预训练基座，专家库包含 256 个在 **Flan v2** 任务上独立微调的 LoRA 专家。
*   **Benchmark (基准方法)**：
    *   *Oracle*：已知任务 ID，直接调用对应专家（性能上限）。
    *   *Shared Expert*：在所有 256 个任务上联合训练的单一 LoRA 模型。
    *   *Arrow*：一种先进的零射（Zero-shot）路由基准方法。
*   **评估指标**：在全部 256 个任务的测试集上计算平均负对数似然损失（Loss）。

### 4. 资源与算力
*   **模型规格**：Phi-2 (2.8B)，LoRA rank=4。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到在 SGD 优化实验中，对学习率进行了搜索（$\{10^{-4}, 10^{-5}, 10^{-3}\}$）并训练了 5 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 256 个任务的广泛测试，实验设计非常详尽。
*   **消融实验**：包括概率平均 vs. 对数几率（Logits）平均、全秩合并 vs. 低秩合并、Top-k 路由敏感度分析、以及专家数量从 1 到 256 的性能演变曲线。
*   **客观性**：对比了均匀权重与学习权重、层相关与全局权重等多种变体，实验对比在相同专家库和基座模型下进行，具有高度的公平性和说服力。

### 6. 主要结论与发现
*   **性能排序**：**路由 (Routing) > 集成 (Ensembling) > 合并 (Merging)**。
*   **路由的优越性**：SGD 优化的路由最接近 Oracle 性能，证明了输入依赖的灵活性对于多任务处理至关重要。
*   **合并的局限性**：简单的参数合并（Merging）效果最差，甚至不如均匀集成，这表明多任务微调模型之间的线性模式连通性较弱。
*   **非均匀增益**：无论是集成还是合并，通过 SGD 学习非均匀的权重系数均比简单的均匀平均有显著提升。
*   **效率优化**：通过专家聚类（如 MBC），可以将专家数量减少 95% 以上（从 256 减至 10），同时保持极高的性能，有效解决了路由的计算开销问题。

### 7. 优点
*   **系统性强**：首次在 PEFT 专家背景下对三种融合策略进行了如此深入的端到端对比。
*   **实战指导意义**：为开发者提供了明确的选择建议（追求性能选路由，追求简单选集成，合并需谨慎）。
*   **创新的路由方案**：提出的层次聚类路由（HC）无需访问原始训练数据即可重构专家库。

### 8. 不足与局限
*   **训练依赖**：性能最好的 SGD 路由和集成方法仍需要一部分多任务数据进行二次训练，并非完全“开箱即用”。
*   **模型规模局限**：实验主要集中在 2.8B 规模的模型，尚未验证这些结论在百亿或千亿级参数模型上的可扩展性。
*   **任务多样性**：虽然任务数量多（256个），但均属于 Flan v2 指令微调范畴，对于代码生成、长文本推理等差异化极大的任务分布验证略显不足。

（完）
