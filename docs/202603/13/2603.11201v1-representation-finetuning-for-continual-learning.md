---
title: Representation Finetuning for Continual Learning
title_zh: 持续学习中的表示微调
authors: "Haihua Luo, Xuming Ran, Tommi Kärkkäinen, Huiyan Xue, Zhonghua Chen, Qi Xu, Fengyu Cong"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11201v1"
tags: ["query:peft"]
score: 9.0
evidence: 将PEFT范式从权重空间转向表示空间
tldr: 针对持续学习中预训练模型微调导致的表示漂移和灾难性遗忘问题，本文提出了CoRe框架。该方法首次将微调范式从权重空间转向表示空间，通过在隐藏表示的低秩线性子空间内进行任务特定的干预，实现了对表示漂移的显式控制。CoRe在保持新任务塑性的同时确保了旧任务的稳定性，具有极高的参数效率，并在多个基准测试中显著超越了现有最先进方法，为持续学习提供了更具可解释性的新路径。
selection_source: fresh_fetch
motivation: 传统的参数高效微调方法在权重空间进行黑盒优化，难以显式控制表示漂移，导致模型在处理持续数据流时容易产生灾难性遗忘。
method: 引入CoRe框架，通过在隐藏表示的低秩线性子空间内执行任务特定的干预，将微调范式从权重空间转移到表示空间。
result: 实验证明CoRe在多个持续学习基准测试中显著优于现有最先进方法，同时保持了极高的参数效率。
conclusion: 表示微调是一种比权重微调更有效且更具可解释性的持续学习范式，能更好地平衡模型的稳定性和塑性。
---

## 摘要
世界本质上是动态的，持续学习旨在使模型能够适应不断演化的数据流。虽然预训练模型在持续学习中表现出了强大的性能，但它们仍需要进行微调以有效适应下游任务。然而，主流的参数高效微调（PEFT）方法通过权重层面的经验性黑盒优化来运行。这些方法缺乏对表示漂移的显式控制，导致在持续学习场景中对领域偏移敏感并产生灾难性遗忘。在这项工作中，我们引入了持续表示学习（CoRe），这是一个首次将微调范式从权重空间转向表示空间的创新框架。与传统方法不同，CoRe 在隐藏表示的低秩线性子空间内执行特定于任务的干预，采用具有显式目标的学习过程，从而在保持新任务塑性的同时确保旧任务的稳定性。通过将更新限制在低秩子空间内，CoRe 实现了卓越的参数效率。在多个持续学习基准测试上的广泛实验表明，CoRe 不仅保持了参数效率，而且显著优于现有的最先进方法。我们的工作将表示微调引入为一种新的、更有效且更具可解释性的持续学习范式。

## Abstract
The world is inherently dynamic, and continual learning aims to enable models to adapt to ever-evolving data streams. While pre-trained models have shown powerful performance in continual learning, they still require finetuning to adapt effectively to downstream tasks. However, prevailing Parameter-Efficient Fine-Tuning (PEFT) methods operate through empirical, black-box optimization at the weight level. These approaches lack explicit control over representation drift, leading to sensitivity to domain shifts and catastrophic forgetting in continual learning scenarios. In this work, we introduce Continual Representation Learning (CoRe), a novel framework that for the first time shifts the finetuning paradigm from weight space to representation space. Unlike conventional methods, CoRe performs task-specific interventions within a low-rank linear subspace of hidden representations, adopting a learning process with explicit objectives, which ensures stability for past tasks while maintaining plasticity for new ones. By constraining updates to a low-rank subspace, CoRe achieves exceptional parameter efficiency. Extensive experiments across multiple continual learning benchmarks demonstrate that CoRe not only preserves parameter efficiency but also significantly outperforms existing state-of-the-art methods. Our work introduces representation finetuning as a new, more effective and interpretable paradigm for continual learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Representation Finetuning for Continual Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在持续学习（Continual Learning, CL）中，预训练模型虽然强大，但仍需微调以适应下游任务。现有的参数高效微调（PEFT）方法（如 Adapter、Prompt Tuning）主要在**权重空间**进行黑盒优化，缺乏对**表示漂移（Representation Drift）**的显式控制。这导致模型在学习新任务时容易干扰旧任务的特征表达，从而引发严重的灾难性遗忘。
*   **研究背景**：传统的微调范式侧重于更新模型参数，而忽略了隐藏表示层面的直接干预。本文旨在探索一种更具可解释性、能显式平衡“塑性-稳定性”的微调新范式。

### 2. 论文提出的方法论：CoRe 框架
*   **核心思想**：提出**持续表示学习（Continual Representation Learning, CoRe）**，首次将微调范式从“权重空间”转向“**表示空间**”。它通过在隐藏表示的低秩线性子空间内进行任务特定的干预来校准特征。
*   **关键技术细节**：
    *   **表示干预公式**：对于输入特征 $e_b$，干预后的特征 $g_\theta(e_b)$ 定义为：
        $g_\theta(e_b) = e_b + R^\top (W e_b + b - R e_b)$
        其中，$R$ 是低秩投影矩阵（定义干预子空间），$W$ 和 $b$ 是可学习的线性变换参数（用于学习校准规则）。
    *   **显式优化目标**：包含两个部分：
        1.  **表示对齐项**：使变换后的表示接近目标理想表示。
        2.  **正交约束项**：保持投影矩阵 $R$ 的正交性，确保子空间干预的几何特性，限制表示漂移的幅度。
*   **算法流程**：在预训练模型（如 ViT）的 Transformer 块中插入 ReFT 模块。训练期间冻结预训练权重，仅更新 $R, W, b$。

### 3. 实验设计
*   **实验场景**：涵盖了持续学习的三大主流场景：
    1.  **任务增量学习 (TIL)**：提供任务标识符。
    2.  **领域增量学习 (DIL)**：类别相同但领域（分布）不断变化。
    3.  **类别增量学习 (CIL)**：最难场景，类别不断增加且推理时无任务标识。
*   **数据集 (Benchmark)**：
    *   使用了超过 20 个数据集，包括 CIFAR100、ImageNet-A/R、CUB200、DomainNet、OfficeHome、EuroSAT、Aircraft 等，涵盖了细粒度分类、领域偏移、大规模场景识别等多种维度。
*   **对比方法**：
    *   **基准**：冻结的 ViT、全量微调 (Full-Finetuning)。
    *   **PEFT 方法**：Adapter、Visual Prompt Tuning (VPT)、SSF (Scaling & Shifting Features)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练配置**：提到了使用 SGD 优化器，初始学习率为 0.05（采用余弦衰减），权重衰减为 0.0005，Batch Size 为 48，每个任务训练 20 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽且具有说服力。
    *   **多场景覆盖**：在 TIL、DIL、CIL 三个场景下均进行了大规模测试。
    *   **鲁棒性测试**：使用了 5 组不同的随机种子来验证结果的稳定性。
    *   **现实挑战模拟**：专门针对**类别不平衡（Class Imbalance）**进行了不同不平衡因子（$\alpha$）的对比实验。
    *   **消融实验**：深入探讨了低秩子空间的秩（Rank）大小、插入 ReFT 的层数对性能的影响。
    *   **骨干网络验证**：分别在 ViT-B/16-IN21K 和 ViT-B/16-IN1K 两种预训练权重下进行了验证。
*   **评价**：实验设计客观、公平，对比基准全面，充分证明了该方法在不同条件下的普适性。

### 6. 主要结论与发现
*   **性能领先**：CoRe 在几乎所有基准测试中均显著优于现有的 PEFT 方法，尤其是在最具挑战性的 CIL 场景下。
*   **参数效率极高**：CoRe 在取得最高准确率的同时，其**可训练参数量是所有对比方法中最少的**（见论文 Fig. 2）。
*   **稳定性强**：通过正交约束和子空间限制，CoRe 有效抑制了表示漂移，在保持学习新知识“塑性”的同时，极好地保留了旧知识的“稳定性”。
*   **抗失衡能力**：在极端的类别不平衡条件下，CoRe 的性能下降幅度远小于其他方法。

### 7. 优点（亮点）
*   **范式突破**：打破了传统的权重微调思维，开创了持续学习中的“表示微调”路径。
*   **理论支撑**：为表示干预提供了数学证明（定理 1），证明了更新幅度受限于子空间的几何结构。
*   **高效率与高性能并存**：通常 PEFT 方法在参数极少时性能会受限，但 CoRe 实现了参数量与准确率的双重优化。
*   **可解释性**：相比黑盒的权重更新，直接在表示空间进行线性校准更具直观意义。

### 8. 不足与局限
*   **架构局限性**：实验主要基于 Transformer 架构（ViT），虽然这是目前的主流，但未讨论该方法在 CNN 等其他架构上的表现。
*   **推理开销**：虽然参数量少，但在每一层都引入了额外的线性变换（$R, W, b$），虽然计算量微乎其微，但在极端低延迟要求的场景下仍需考虑。
*   **超参数敏感性**：秩（Rank）的选择对性能有一定影响（实验显示 Rank=8 最优），在不同规模的模型或任务中可能需要重新调优。

（完）
