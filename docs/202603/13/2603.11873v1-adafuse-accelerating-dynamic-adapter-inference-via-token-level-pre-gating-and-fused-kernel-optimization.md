---
title: "AdaFuse: Accelerating Dynamic Adapter Inference via Token-Level Pre-Gating and Fused Kernel Optimization"
title_zh: AdaFuse：通过 Token 级预门控与融合算子优化加速动态适配器推理
authors: "Qiyang Li, Rui Kong, Yuchen Li, Hengyi Cai, Shuaiqiang Wang, Linghe Kong, Guihai Chen, Dawei Yin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11873v1"
tags: ["query:peft"]
score: 9.0
evidence: 加速 LoRA 动态适配器推理
tldr: 针对动态适配器（如LoRA+MoE）在LLM推理中因频繁CUDA内核启动导致的严重延迟问题，本文提出AdaFuse框架。该框架通过算法与硬件协同设计，引入Token级预门控策略，在Token处理前进行全局路由决策，并开发定制化CUDA内核实现参数融合切换。实验表明，AdaFuse在保持模型精度的同时，将解码延迟降低了2.4倍以上，显著提升了推理效率。
selection_source: fresh_fetch
motivation: 动态适配器推理中碎片化且顺序执行的CUDA内核启动导致了严重的性能瓶颈，使得解码速度大幅下降。
method: 提出Token级预门控策略以静态化执行路径，并结合定制的融合CUDA内核将选定的适配器参数一次性合并至主干模型。
result: 在主流开源大模型上的实验显示，AdaFuse在维持SOTA精度的前提下，将解码延迟缩减了2.4倍以上。
conclusion: AdaFuse通过软硬件协同优化，成功解决了动态适配器的推理效率难题，实现了模型能力与推理速度的平衡。
---

## 摘要
将混合专家模型 (MoE) 等动态稀疏结构与参数高效适配器（如 LoRA）相结合，是增强大语言模型 (LLM) 的一种强大技术。然而，这种架构增强带来了沉重的代价：尽管计算量增加极小，但推理延迟往往会激增，导致解码速度降低 2.5 倍以上。通过细粒度的性能分析，我们发现主要的瓶颈不在于计算本身，而在于传统动态路由所需的碎片化、顺序化 CUDA 算子 (kernel) 启动所带来的严重开销。为了应对这一挑战，我们推出了 AdaFuse，这是一个基于算法与底层硬件系统紧密协同设计的框架，旨在实现高效的动态适配器执行。与传统的逐层或逐块路由不同，AdaFuse 采用了一种 Token 级预门控策略，在处理 Token 之前，为所有适配器层做出单一的全局路由决策。这种“一次决策，处处应用”的方法有效地使每个 Token 的执行路径静态化，为整体优化创造了机会。我们利用这一点开发了一个自定义 CUDA 算子，执行融合切换操作，在单次高效处理中将所有选定的 LoRA 适配器参数合并到骨干模型中。在流行开源 LLM 上的实验结果表明，AdaFuse 在保持与最先进动态适配器相当的准确率的同时，将解码延迟大幅降低了 2.4 倍以上，从而弥合了模型能力与推理效率之间的差距。

## Abstract
The integration of dynamic, sparse structures like Mixture-of-Experts (MoE) with parameter-efficient adapters (e.g., LoRA) is a powerful technique for enhancing Large Language Models (LLMs). However, this architectural enhancement comes at a steep cost: despite minimal increases in computational load, the inference latency often skyrockets, leading to decoding speeds slowing by over 2.5 times. Through a fine-grained performance analysis, we pinpoint the primary bottleneck not in the computation itself, but in the severe overhead from fragmented, sequential CUDA kernel launches required for conventional dynamic routing. To address this challenge, we introduce AdaFuse, a framework built on a tight co-design between the algorithm and the underlying hardware system to enable efficient dynamic adapter execution. Departing from conventional layer-wise or block-wise routing, AdaFuse employs a token-level pre-gating strategy, which makes a single, global routing decision for all adapter layers before a token is processed. This "decide-once, apply-everywhere" approach effectively staticizes the execution path for each token, creating an opportunity for holistic optimization. We capitalize on this by developing a custom CUDA kernel that performs a fused switching operation, merging the parameters of all selected LoRA adapters into the backbone model in a single, efficient pass. Experimental results on popular open-source LLMs show that AdaFuse achieves accuracy on par with state-of-the-art dynamic adapters while drastically cutting decoding latency by a factor of over 2.4x, thereby bridging the gap between model capability and inference efficiency.

---

## 论文详细总结（自动生成）

### AdaFuse：通过 Token 级预门控与融合算子优化加速动态适配器推理

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：动态适配器（如 LoRA 与 Mixture-of-Experts 的结合）虽然在提升大语言模型（LLM）的参数效率和任务适应性方面表现出色，但其**推理延迟极高**。
*   **研究背景**：尽管动态适配器仅增加了 1%-5% 的参数量和不到 1% 的计算复杂度（FLOPs），但实际解码延迟却增加了 250% 到 950%。
*   **根本原因**：论文通过细粒度分析发现，瓶颈不在于计算量，而在于**碎片化的 CUDA 内核启动（Kernel Launches）**。传统的动态路由（逐层或逐块路由）导致每一层都需要多次顺序调用 CUDA 内核，其上下文切换开销远超实际计算时间。

#### 2. 方法论：核心思想与关键技术
AdaFuse 采用了**算法与硬件系统协同设计**的思想，核心由两部分组成：
*   **Token 级预门控策略（Token-Level Pre-Gating）**：
    *   **核心思想**：改变传统的“逐层路由”模式，改为“一次决策，处处应用”。
    *   **技术细节**：在模型的第一层设置一个全局路由（Top-2 Router），为当前 Token 决定其在后续所有层中激活的适配器专家。这使得每个 Token 的执行路径在进入深层网络前就已静态化。
*   **融合算子优化（Fused Kernel Optimization - SGMM）**：
    *   **核心思想**：利用预门控带来的静态路径，将适配器参数直接合并到主干模型（Backbone）中。
    *   **关键技术 (SGMM)**：开发了“分段收集矩阵乘法”（Segmented Gather Matrix Multiplication）内核。该内核可以在单次 CUDA 调用中，将所有层选定的 LoRA 适配器权重与原始权重进行融合（Merge）和拆卸（Unmerge）。
    *   **流程**：Token 输入 -> 路由决策 -> SGMM 融合适配器 -> 执行标准推理 -> SGMM 拆卸（为下一个 Token 做准备）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **通用能力**：SlimORCA、Magicoder、MetaMathQA。
    *   **领域特定任务**：ScienceQA、CommonsenseQA、OpenbookQA。
    *   **推理效率评估**：使用 ShareGPT 数据集模拟真实用户查询。
*   **Benchmark**：ARC、HellaSwag、MMLU、TruthfulQA、Winogrande、MT-Bench。
*   **对比方法**：
    *   基础模型：Llama2-7B、Mistral-7B。
    *   静态适配器：LoRA。
    *   动态适配器：MOLA（逐层）、PESC（逐块）、MoRAL（逐层）。

#### 4. 资源与算力
*   **硬件环境**：论文在实验部分展示了详细的延迟（ms/token）和显存（GiB）占用数据。
*   **具体说明**：文中**未明确指出**训练和测试所使用的具体 GPU 型号（如 A100 或 H100）、GPU 数量以及具体的训练总时长。这在系统类论文中略显遗憾，但其提供的相对加速比（2.4x）具有参考价值。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 2 个主流开源模型系列（Llama2, Mistral）。
    *   进行了 5 项通用能力评测和 3 项领域特定任务评测。
    *   包含详细的推理性能对比（延迟与显存）。
    *   **消融实验**：专门验证了 SGMM 内核对性能的贡献，对比了“简单合并”与“SGMM 融合”的差异。
*   **充分性评价**：实验设计较为全面，不仅关注了系统层面的加速，还通过多维度准确率测试证明了“预门控”简化算法并不会显著损害模型性能。对比基准包含了近年来主要的 SOTA 动态适配器方法，具有较强的说服力。

#### 6. 主要结论与发现
*   **性能飞跃**：AdaFuse 将动态适配器的解码延迟降低了 **2.4 倍以上**。
*   **延迟控制**：相比于原始 Backbone 模型，AdaFuse 的额外延迟开销仅为 **29%**，而传统动态适配器（如 MOLA）的开销高达 954%。
*   **精度保持**：在通用和特定领域任务上，AdaFuse 的准确率与 PESC、MoRAL 等复杂的逐层路由方法持平，甚至在某些任务（如 MMLU）上表现更优。
*   **显存友好**：通过 SGMM 的原位（In-place）操作，显存开销仅比基础模型增加约 7%。

#### 7. 优点与亮点
*   **软硬协同设计**：不只是单纯改进算法，而是深入到 CUDA 内核层面解决系统瓶颈，这是该论文最突出的亮点。
*   **解决实际痛点**：直接针对动态适配器“学术表现好、工业部署难”的延迟问题给出了切实可行的方案。
*   **架构简洁**：Token 级预门控简化了路由逻辑，使得动态模型在推理时表现得像静态模型一样高效。

#### 8. 不足与局限
*   **算法假设风险**：预门控假设“所有层共享相同的路由决策”是有效的。虽然实验证明了其可行性，但在极度复杂的跨领域任务中，不同层可能需要不同的专家知识，这种简化可能会存在表达能力的上限。
*   **Prefilling 阶段未优化**：论文明确提到目前主要针对 Decoding（解码）阶段进行优化，对于 Prefilling（预填充/首字延迟）阶段尚未实现专门的加速。
*   **硬件依赖性**：SGMM 内核的性能高度依赖于 GPU 架构和算子实现，其在不同代际 GPU 上的通用性和移植成本未详细讨论。

（完）
