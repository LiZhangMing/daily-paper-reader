---
title: "MIL-PF: Multiple Instance Learning on Precomputed Features for Mammography Classification"
title_zh: MIL-PF：基于预计算特征的多示例学习用于乳腺 X 线摄影分类
authors: "Nikola Jovišić, Milica Škipina, Nicola Dall'Asen, Dubravko Ćulibrk"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09374v1"
tags: ["query:peft"]
score: 8.0
evidence: 仅训练一个小的任务特定模块（4万参数）
tldr: 针对乳腺 X 线摄影图像分辨率高、多视图且标注稀疏导致端到端微调成本高昂的问题，本文提出 MIL-PF 框架。该方法利用冻结的基础模型提取预计算特征，仅训练一个轻量级（约4万参数）的多实例学习聚合头。通过注意力机制建模全局组织背景与稀疏局部病灶，在显著降低计算复杂度的同时，在临床规模数据集上达到了最先进的分类性能，为大规模医学影像分析提供了高效方案。
selection_source: fresh_fetch
motivation: 高分辨率乳腺影像的端到端微调受限于计算资源、弱监督标注以及复杂的多视图结构。
method: 利用冻结的基础模型预计算语义表示，并仅训练一个轻量级的注意力机制多实例学习聚合模块。
result: 在临床规模的乳腺癌分类任务中取得了最先进的性能，同时大幅降低了训练复杂度和成本。
conclusion: MIL-PF 证明了结合预训练基础模型特征与轻量级任务特定聚合器是处理复杂医学影像的高效路径。
---

## 摘要
现代基础模型提供了极具表现力的视觉表征，但由于标注有限且存在弱监督，将其应用于高分辨率医学影像仍具挑战。尤其是乳腺 X 线摄影，其特点是图像尺寸大、多视图研究多变且主要采用乳腺级标签，这使得端到端微调在计算上非常昂贵且往往不切实际。我们提出了基于预计算特征的多示例学习（MIL-PF），这是一个可扩展的框架，将冻结的基础编码器与轻量级 MIL 头部相结合，用于乳腺 X 线摄影分类。通过预计算语义表征并仅训练一个小型特定任务聚合模块（4 万个参数），该方法无需重新训练大型骨干网络即可实现高效的实验和适配。该架构通过基于注意力的聚合，显式地对全局组织背景和稀疏的局部病变信号进行建模。MIL-PF 在临床规模上实现了最先进的分类性能，同时大幅降低了训练复杂度。我们发布了代码以实现完全的可重复性。

## Abstract
Modern foundation models provide highly expressive visual representations, yet adapting them to high-resolution medical imaging remains challenging due to limited annotations and weak supervision. Mammography, in particular, is characterized by large images, variable multi-view studies and predominantly breast-level labels, making end-to-end fine-tuning computationally expensive and often impractical. We propose Multiple Instance Learning on Precomputed Features (MIL-PF), a scalable framework that combines frozen foundation encoders with a lightweight MIL head for mammography classification. By precomputing the semantic representations and training only a small task-specific aggregation module (40k parameters), the method enables efficient experimentation and adaptation without retraining large backbones. The architecture explicitly models the global tissue context and the sparse local lesion signals through attention-based aggregation. MIL-PF achieves state-of-the-art classification performance at clinical scale while substantially reducing training complexity. We release the code for full reproducibility.

---

## 论文详细总结（自动生成）

这是一份关于论文《MIL-PF: Multiple Instance Learning on Precomputed Features for Mammography Classification》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
乳腺 X 线摄影（Mammography）在计算机辅助诊断中面临三大挑战：
*   **高分辨率与计算压力**：单张图像分辨率极高（可达 4708×5844），传统端到端微调大型视觉模型（如 ViT）的显存和计算成本极高。
*   **弱监督标注**：临床通常只有“乳腺级”标签（即整个乳腺是否患病），缺乏精确的像素级或病灶级标注。
*   **多视图结构**：诊断通常需要结合多个角度（如 CC 和 MLO 视图）的图像，形成一个“包”（Bag）结构。
**研究动机**：探索是否可以利用冻结的（Frozen）通用视觉基础模型提取特征，仅通过训练一个极轻量级的聚合模块来实现高效、高精度的分类。

### 2. 方法论：MIL-PF 框架
核心思想是将特征提取与任务学习解耦，采用**多示例学习（MIL）**处理多视图和局部病灶。
*   **特征预计算（Feature Precomputing）**：使用预训练好的冻结编码器（如 DINOv2 或 MedSigLIP）将原始图像和切片（Tiles）转化为低维嵌入向量（Embeddings），并存储为数据集。
*   **双流架构**：
    *   **全局流（Global Stream）**：对整张图像进行编码，捕捉全局组织结构和背景上下文。
    *   **局部流（Local Stream）**：将图像切分为网格切片，提取局部高分辨率特征，以捕捉稀疏的病灶信号。
*   **聚合机制**：
    *   **全局聚合**：对多个视图的全局特征进行池化。
    *   **局部聚合（关键创新）**：采用 **Perceiver 风格的交叉注意力机制**。使用一个可学习的潜在向量（Latent Query）去查询所有切片特征，通过注意力权重自动识别并提取最相关的病灶信息。
*   **轻量级头部**：最终的分类头仅包含约 **4 万个参数**，通过后期融合（Late Fusion）合并全局和局部摘要进行预测。

### 3. 实验设计
*   **数据集**：
    *   **EMBED**：约 50 万张图像，极具多样性的真实临床数据集。
    *   **VinDr-Mammo**：大型公开基准数据集。
    *   **RSNA**：乳腺癌筛选竞赛数据集。
*   **任务**：基于 BI-RADS 分级的恶性肿瘤分类（乳腺级别）。
*   **对比方法（Baselines）**：包括 GMIC、FPN-AbMIL、FPN-SetTrans 等近年来的 SOTA（最先进）模型。
*   **评估指标**：AUC（曲线下面积）、bACC（平衡准确率）、mAP（平均精度，用于检测）、以及临床相关的 Spec@Sens=0.9（灵敏度为 0.9 时的特异性）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 40GB GPU**。
*   **训练效率**：由于特征已预计算，整个 MIL-PF 头部的训练非常快。在 EMBED 这样的大规模数据集上，单次运行仅需 **5-7 分钟**。
*   **推理成本**：单次前向传播（每个乳腺）仅需约 **200 万次浮点运算（2M FLOPS）**。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流数据集上进行了验证，涵盖了从几千到几十万样本的不同规模。
*   **消融实验**：详细对比了不同基础编码器（DINOv2, MedSigLIP, MammoCLIP 等）以及不同的聚合策略（均值池化、最大池化、注意力池化）。
*   **统计可靠性**：针对每个实验进行了 **36 次独立运行**，以观察方差并确保结果的鲁棒性，而非偶然的“幸运运行”。实验设计客观、公平，对比基准均在相同数据分割下重新训练。

### 6. 主要结论与发现
*   **冻结编码器的强大性能**：即使不进行端到端微调，通用的视觉基础模型（如 DINOv2）在乳腺摄影领域的泛化能力也极其出色，甚至优于某些专门在医学数据上预训练的模型。
*   **轻量化可行性**：仅需 4 万个可训练参数即可在临床规模任务上达到 SOTA 性能。
*   **领域对齐的重要性**：通过设计符合乳腺摄影归纳偏置（如全局+局部双流）的架构，可以显著提升分类效果。

### 7. 优点
*   **极高效率**：大幅降低了科研门槛，使资源有限的团队也能处理大规模高分辨率医学影像。
*   **可解释性**：通过注意力图（Attention Maps）可以直观地观察到模型关注的病灶区域，具有临床参考价值。
*   **模块化**：编码器可随基础模型的发展轻松替换，无需重构整个管线。

### 8. 不足与局限
*   **小病灶检测精度有限**：由于输入切片尺寸较大（如 518x518），模型在检测极小病灶（如微小钙化点）时的定位精度（mAP）仍有提升空间。
*   **依赖预训练模型**：性能高度依赖于所选基础编码器的表征能力，若基础模型存在领域偏差，可能会影响结果。
*   **静态特征限制**：预计算特征意味着无法在训练过程中根据特定任务调整特征提取器（即缺乏端到端的特征演进）。

（完）
