---
title: Noise-aware few-shot learning through bi-directional multi-view prompt alignment
title_zh: 通过双向多视图提示对齐的噪声感知少样本学习
authors: "Lu Niu, Cheng Xue"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11617v1"
tags: ["query:peft"]
score: 8.0
evidence: 视觉语言模型的噪声感知提示微调
tldr: 针对视觉语言模型在少样本学习中易受噪声标签干扰的问题，本文提出NA-MVP框架。该框架通过多视图提示与非平衡最优传输实现细粒度区域对齐并抑制噪声区域，结合双向提示设计捕捉互补的清洁与噪声感知线索，并利用对齐引导的策略修正错误标签。实验证明，该方法在多个噪声基准上显著优于现有技术，提升了噪声监督下的少样本学习鲁棒性。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型少样本学习方法难以在存在噪声标签的情况下有效建模细粒度语义并区分清洁信号与噪声信号。
method: 提出NA-MVP框架，利用多视图提示、非平衡最优传输以及双向提示设计来实现细粒度的区域对齐和自适应的标签修正。
result: 在合成和真实世界的噪声基准测试中，NA-MVP 始终优于最先进的基准方法，展现了极强的鲁棒性。
conclusion: 该研究证明了通过细粒度区域对齐和双向提示建模可以有效解决少样本学习中的噪声干扰问题。
---

## 摘要
视觉语言模型通过提示微调（prompt tuning）提供了强大的少样本能力，但仍容易受到噪声标签的影响，这可能会破坏提示并降低跨模态对齐的效果。现有方法面临困难，因为它们通常缺乏建模细粒度语义线索以及自适应分离干净信号与噪声信号的能力。为了应对这些挑战，我们提出了 NA-MVP，这是一个通过双向多视图提示对齐进行噪声感知少样本学习的框架。NA-MVP 基于一个关键的概念转变：鲁棒的提示学习需要从全局匹配转向区域感知对齐，从而明确区分干净线索与噪声线索。为了实现这一点，NA-MVP 采用了：(1) 多视图提示结合不平衡最优传输（unbalanced optimal transport），以实现细粒度的补丁到提示（patch-to-prompt）对应关系，同时抑制不可靠区域；(2) 双向提示设计，捕捉互补的面向干净数据的线索和噪声感知线索，使模型能够专注于稳定的语义；以及 (3) 一种对齐引导的选择性细化策略，利用最优传输仅纠正错误标记的样本，同时保留可靠数据。在合成和真实世界噪声基准上的实验表明，NA-MVP 一致优于最先进的基准方法，证实了其在噪声监督下实现鲁棒少样本学习的有效性。

## Abstract
Vision-language models offer strong few-shot capability through prompt tuning but remain vulnerable to noisy labels, which can corrupt prompts and degrade cross-modal alignment. Existing approaches struggle because they often lack the ability to model fine-grained semantic cues and to adaptively separate clean from noisy signals. To address these challenges, we propose NA-MVP, a framework for Noise-Aware few-shot learning through bi-directional Multi-View Prompt alignment. NA-MVP is built upon a key conceptual shift: robust prompt learning requires moving from global matching to region-aware alignment that explicitly distinguishes clean cues from noisy ones. To realize this, NA-MVP employs (1) multi-view prompts combined with unbalanced optimal transport to achieve fine-grained patch-to-prompt correspondence while suppressing unreliable regions; (2) a bi-directional prompt design that captures complementary clean-oriented and noise-aware cues, enabling the model to focus on stable semantics; and (3) an alignment-guided selective refinement strategy that uses optimal transport to correct only mislabeled samples while retaining reliable data. Experiments on synthetic and real-world noisy benchmarks demonstrate that NA-MVP consistently outperforms state-of-the-art baselines, confirming its effectiveness in enabling robust few-shot learning under noisy supervision.

---

## 论文详细总结（自动生成）

以下是对论文《Noise-aware few-shot learning through bi-directional multi-view prompt alignment》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在少样本学习（Few-shot Learning）场景下，视觉语言模型（VLM，如 CLIP）通过提示微调（Prompt Tuning）展现了卓越的性能。然而，现实数据往往包含**噪声标签**。在样本极少的情况下，即使是少量的错误标注也会严重干扰梯度更新，导致模型学习到错误的关联。
现有方法（如 CoOp, NLPrompt 等）存在三大局限：
*   **视角单一**：通常仅使用一两个全局提示，无法捕捉图像中细粒度的语义线索。
*   **负样本监督僵化**：依赖显式的负标签，在噪声环境下容易引入错误引导。
*   **去噪粗糙**：多依赖固定阈值或简单的伪标签，容易导致错误传播。
**研究动机**：将鲁棒提示学习从“全局匹配”转向“区域感知对齐”，通过区分干净与噪声线索来增强模型的鲁棒性。

### 2. 方法论：核心思想与关键技术
论文提出了 **NA-MVP** 框架，其核心由三个模块组成：
*   **双向多视图提示设计（Bi-directional Multi-view Prompts）**：
    *   为每个类别构建两组可学习提示：**面向干净的提示（Clean-oriented）**和**噪声感知的提示（Noise-aware）**。
    *   每组包含多个视图（$N$个提示），以捕捉类内的多样化语义。
    *   将非目标类视为隐式负样本，避免了显式负标签的僵化。
*   **基于不平衡最优传输（UOT）的细粒度对齐**：
    *   利用 UOT 将图像的局部补丁（Patches）与多视图提示进行对齐。
    *   **关键细节**：UOT 放宽了质量守恒约束，允许部分匹配。这使得模型能够自动下调（downweight）噪声区域的权重，仅让可靠的视觉特征与语义提示对齐。
*   **对齐引导的选择性标签细化**：
    *   **自适应阈值**：根据干净提示与噪声提示的相对对齐强度，为每个样本计算动态阈值 $\phi$。
    *   **选择性修正**：仅对识别为“不可靠”的样本使用**经典最优传输（Classical OT）**进行标签修正。经典 OT 通过全局图像特征与文本特征的分布对齐，确保标签分配的全局一致性。

### 3. 实验设计
*   **数据集**：
    *   **合成噪声基准**：Caltech101, DTD, Flowers102, OxfordPets, UCF101。
    *   **真实世界噪声基准**：Food101N。
*   **噪声设置**：对称噪声（Symmetric）和非对称噪声（Asymmetric），噪声比例从 12.5% 到 75% 不等。
*   **Benchmark 与对比方法**：
    *   基础方法：CoOp。
    *   噪声鲁棒方法：CoOp+GCE, JoAPR, NLPrompt, DEFT。
*   **实验设置**：统一采用 16-shot 设置，使用 ResNet-50 作为图像编码器。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA GeForce RTX 4090 GPU** 上完成。
*   **训练细节**：
    *   优化器：SGD，学习率 0.002，动量 0.9。
    *   训练轮数：50 Epochs。
    *   参数量：文本编码器为 63M 参数的 Transformer，图像编码器冻结。
*   **耗时**：文中未明确给出总训练时长，但提到使用了 Sinkhorn 算法和 Dykstra 算法的快速实现，以降低 OT 计算的开销。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集上进行了广泛测试，涵盖了从低到极高（75%）的噪声水平。
*   **消融实验**：详细分析了双向提示、多视图数量（$N=1,2,4,8$）、UOT 的有效性以及自适应阈值的作用。
*   **客观性与公平性**：所有实验均取三次随机种子的平均值；对比基准涵盖了近年（2022-2025）的最先进方法，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能领先**：NA-MVP 在所有数据集和噪声比例下均优于 SOTA 方法，尤其在**高噪声（>50%）**环境下优势更明显（例如在 DTD 75% 噪声下比 NLPrompt 高出约 9%）。
*   **多视图价值**：增加提示视图数量（至 $N=4$）能显著提升性能，证明了细粒度语义建模的重要性。
*   **UOT 的优越性**：相比于经典 OT 或 KL 散度，UOT 在处理局部特征对齐时能更好地抑制噪声干扰。
*   **真实场景有效性**：在 Food101N 上的表现证明了该方法处理自然噪声的能力。

### 7. 优点（亮点）
*   **概念创新**：将噪声处理从全局标签层面深入到图像局部区域与语义视图的对齐层面。
*   **自适应性强**：通过双向提示产生的对齐信号自动推导阈值，避免了手动设置超参数的繁琐。
*   **鲁棒性极佳**：在极端噪声（75%）下依然能保持相对稳定的分类准确率。

### 8. 不足与局限
*   **计算复杂度**：尽管使用了快速 Sinkhorn 算法，但相比于简单的线性分类器，引入 UOT 和多次 OT 迭代仍会增加训练时的计算负担。
*   **依赖预训练质量**：该方法高度依赖 CLIP 等预训练模型的初始对齐能力，若基础模型在某些特定领域表现较差，提示对齐的效果可能受限。
*   **推理延迟**：推理时需要计算多个提示视图的对齐得分，可能在实时性要求极高的场景下存在挑战。

（完）
