---
title: "Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning"
title_zh: 简单方案行之有效：视觉-语言-动作模型是强化学习下的天然持续学习者
authors: "Jiaheng Hu, Jay Shim, Chen Tang, Yoonchang Sung, Bo Liu, Peter Stone, Roberto Martin-Martin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11653v1"
tags: ["query:peft"]
score: 10.0
evidence: 结合低秩自适应（LoRA）的顺序微调
tldr: 本研究探讨了视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现。尽管传统观点认为顺序微调（Seq. FT）会导致灾难性遗忘，但作者发现，结合低秩自适应（LoRA）的简单顺序微调在大型预训练VLA模型上表现极其出色。通过在多个基准测试上的实验，研究证明了这种简单方法不仅具有高塑性，且几乎没有遗忘，甚至优于复杂的CRL策略，为大模型时代的持续学习提供了新见解。
selection_source: fresh_fetch
motivation: 旨在挑战顺序微调在持续强化学习中必然导致灾难性遗忘的传统认知，探索大模型在动态环境中的自适应能力。
method: 在大型预训练VLA模型上采用结合低秩自适应（LoRA）的简单顺序微调方法，并利用在线强化学习进行持续训练。
result: 实验表明，简单的顺序微调在保持高塑性的同时几乎不产生遗忘，且在零样本泛化能力上优于许多复杂的持续学习算法。
conclusion: 大型预训练模型、参数高效微调与在线强化学习的协同作用，使得简单的顺序微调成为VLA模型持续学习的强大且可扩展的方案。
---

## 摘要
针对视觉-语言-动作（VLA）模型的持续强化学习（CRL）是实现能在开放式、演化环境中适应的自改进具身智能体的一个极具前景的方向。然而，持续学习的传统观点认为，朴素的顺序微调（Seq. FT）会导致灾难性遗忘，因此需要复杂的 CRL 策略。在这项工作中，我们退后一步，对三种模型和五个具有挑战性的终身强化学习基准上的大规模预训练 VLA 的 CRL 进行了系统研究。我们发现，与既有观念相反，结合低秩自适应（LoRA）的简单顺序微调表现异常强劲：它实现了高可塑性，几乎没有遗忘，并保持了强大的零样本泛化能力，经常优于更复杂的 CRL 方法。通过详细分析，我们表明这种鲁棒性源于大规模预训练模型、参数高效自适应和同策略（on-policy）强化学习之间的协同作用。这些组件共同重塑了稳定性-可塑性权衡，使持续适应既稳定又具可扩展性。我们的结果将顺序微调定位为 VLA 持续强化学习的一种强大方法，并为大模型时代的终身学习提供了新的见解。代码可在 github.com/UT-Austin-RobIn/continual-vla-rl 获取。

## Abstract
Continual Reinforcement Learning (CRL) for Vision-Language-Action (VLA) models is a promising direction toward self-improving embodied agents that can adapt in openended, evolving environments. However, conventional wisdom from continual learning suggests that naive Sequential Fine-Tuning (Seq. FT) leads to catastrophic forgetting, necessitating complex CRL strategies. In this work, we take a step back and conduct a systematic study of CRL for large pretrained VLAs across three models and five challenging lifelong RL benchmarks. We find that, contrary to established belief, simple Seq. FT with low-rank adaptation (LoRA) is remarkably strong: it achieves high plasticity, exhibits little to no forgetting, and retains strong zero-shot generalization, frequently outperforming more sophisticated CRL methods. Through detailed analysis, we show that this robustness arises from a synergy between the large pretrained model, parameter-efficient adaptation, and on-policy RL. Together, these components reshape the stability-plasticity trade-off, making continual adaptation both stable and scalable. Our results position Sequential Fine-Tuning as a powerful method for continual RL with VLAs and provide new insights into lifelong learning in the large model era. Code is available at github.com/UT-Austin-RobIn/continual-vla-rl.

---

## 论文详细总结（自动生成）

以下是对论文《Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：构建通用的具身智能体需要其具备在不断演变的环境中持续学习和自我改进的能力。
*   **核心问题**：传统的持续学习（Continual Learning）观点认为，简单的“顺序微调”（Sequential Fine-Tuning, Seq. FT）会导致严重的“灾难性遗忘”。为了克服这一点，现有的持续强化学习（CRL）方法通常非常复杂（如引入正则化、重放缓冲区或参数隔离）。
*   **整体含义**：本研究挑战了这一传统认知。作者系统地研究了大型预训练视觉-语言-动作（VLA）模型在持续强化学习中的表现，发现**大模型 + 参数高效微调（LoRA）+ 在线强化学习（On-policy RL）**这一组合天然地具有抵抗遗忘的能力，简单的顺序微调方案在实际效果上往往优于复杂的专门算法。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：无需复杂的持续学习策略，仅通过简单的顺序微调即可实现高效的持续学习。
*   **关键技术细节**：
    *   **VLA 模型基础**：使用预训练的大规模 VLA 模型（如 OpenVLA, Pi-0），这些模型具备强大的视觉感知和语言理解能力。
    *   **LoRA（低秩自适应）**：仅更新极少数新增的低秩矩阵参数（$W = W_0 + BA$），保持原始预训练权重 $W_0$ 冻结。这限制了参数更新的范围，保护了原有知识。
    *   **同策略强化学习（On-policy RL）**：采用 **GRPO（Group Relative Policy Optimization）** 算法进行后训练。
    *   **协同效应分析**：论文指出稳定性源于三个维度的协同：
        1.  **目标维度（RL）**：在线 RL 仅在当前策略支持的动作空间内更新，产生隐式的 KL 正则化，防止策略漂移。
        2.  **约束维度（LoRA）**：将更新限制在低秩子空间，防止单层权重发生剧烈结构变化。
        3.  **容量维度（VLA）**：大模型的高维空间存在大量“零空间”（Null Space），新任务的梯度更新与旧任务知识的干扰概率极低（基于 Fisher 信息矩阵分析）。

### 3. 实验设计
*   **基准测试（Benchmarks）**：涵盖了 5 个挑战性场景：
    *   **LIBERO**（Object, Spatial, Long-horizon）：机器人操纵任务，侧重知识迁移。
    *   **RoboCasa**：复杂的厨房场景任务。
    *   **ManiSkill**：基于物理引擎的物体操纵任务。
*   **对比方法**：
    *   **基准**：顺序微调（Seq. FT）、多任务训练（Multitask Oracle，上限）。
    *   **经典 CRL 算法**：EWC（正则化）、Expert Replay（专家重放）、Dark Experience Replay（DER，逻辑重放）、DWE（参数隔离）。
    *   **大模型适配方法**：SLCA（层级学习率）、RETAIN（权重合并）。
*   **评估指标**：平均成功率（AVG）、负向后向迁移（NBT，即遗忘率）、前向迁移（FWT）、零样本成功率（ZS）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号和数量（如 A100/H100 的具体枚数）。
*   **训练规模**：提到训练涉及数十亿参数的模型（7B 级别），每个任务训练约 10,240 个 Episode，全局 Batch Size 为 8192。作者提到实验得到了 UT Austin 机器学习实验室的计算资源支持，暗示了较高的算力需求。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。涵盖了 3 种不同的 VLA 架构（OpenVLA-OFT, Pi-0, OpenVLA），在 5 个不同的基准测试上运行。
*   **鲁棒性测试**：额外进行了环境扰动实验（相机角度、光照、机器人初始位置变化）和任务顺序敏感性测试。
*   **消融实验**：对比了移除 RL（改用有监督微调 SFT）、使用小模型（12M 参数）、移除 LoRA（全参数微调）的情况。
*   **客观性**：所有实验均采用 3 个随机种子取平均值，并提供了详细的超参数列表，具有较高的客观性和可重复性。

### 6. 主要结论与发现
*   **简单方案胜出**：结合 LoRA 的顺序微调在几乎所有指标上都表现强劲，遗忘率极低（NBT < 2%），且塑性（学习新任务的能力）极佳。
*   **零样本能力保持**：Seq. FT 不仅能学会新任务，还能保持甚至增强模型对未见任务的零样本泛化能力，表现甚至优于多任务训练。
*   **遗忘的真相**：在 VLA 领域，遗忘并非不可避免。只要满足“大模型 + LoRA + 在线 RL”这三个条件，灾难性遗忘就会自然消失。

### 7. 优点
*   **实用性极强**：为具身智能的持续学习提供了一个极简且高效的“配方”，降低了系统实现的复杂性。
*   **理论与实证结合**：不仅展示了实验结果，还通过 Fisher 信息矩阵、有效秩（Effective Rank）和信息论视角解释了为何该方案有效。
*   **打破思维定式**：有力地反驳了“顺序微调必然导致遗忘”的传统 CRL 假设。

### 8. 不足与局限
*   **训练效率差距**：虽然 Seq. FT 最终能达到多任务训练的水平，但在相同步数下，其平均成功率仍有约 5% 的微小差距（需通过增加训练时长弥补）。
*   **任务感知假设**：实验假设任务指令（语言）是明确提供的，未深入探讨任务边界模糊或指令缺失时的表现。
*   **仿真与现实的鸿沟**：实验主要在仿真环境中完成，虽然做了鲁棒性扰动，但在真实物理世界中的表现（如硬件磨损、传感器噪声）仍待验证。
*   **算法范围**：主要关注在线（On-policy）RL，对于离线（Off-policy）RL 在此框架下的表现探讨较少。

（完）
